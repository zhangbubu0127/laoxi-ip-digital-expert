# P0 最小闭环实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 跑通「老板在飞书群发需求 → 主控调度派单 → 专家产出 → 回群 → 排期表更新」的最小闭环（P0）。

**Architecture:** 皮肤层（lark-cli 收/发消息、身份解析）与大脑层（主控调度、专家逻辑）通过管道协议解耦。P0 用单 bot + 身份标签，专家逻辑先用「规则版」（读知识库文件产出），后续 P1 增强。

**Tech Stack:** Python 3（标准库 unittest，零依赖）+ lark-cli（已装已登录）+ 本地 Markdown 文件存储。

## Global Constraints

- 皮肤层只做收发+身份解析+权限拦截，不做业务判断；大脑层不直接调 lark-cli。
- 智能体不能自动标「已发」——已发必须人工确认（发片同事回「已发布」）。
- 不确定事实走「问老板」，禁止静默编造。
- 知识库事实层只读；排期表主控唯一写权限。
- 异常一律「问老板」或「明说失败」。
- 代码默认无注释；注释只在 WHY 非显然时写。
- 不安装新的全局依赖（红线）。
- **GitHub 安全**（用户已确认会上传 GitHub）：
  - `config/roles.json`（含飞书 open_id）**必须 gitignore**，不提交。
  - lark-cli 凭证在 `~/.lark-cli/config.json`，不在项目内，天然不入库；不要复制进项目。
  - 任何含 token/appSecret/open_id 的文件不入库。仓库公开前先自查。

---

### Task 0: 项目脚手架 + git

**Files:**
- Create: `/Users/User/Desktop/老席留学ip数字专家/CLAUDE.md`（项目规则）
- Create: `/Users/User/Desktop/老席留学ip数字专家/.gitignore`（忽略 config/roles.json 等敏感文件）
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/__init__.py`
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/skin/__init__.py`
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/brain/__init__.py`
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/brain/experts/__init__.py`
- Create: `/Users/User/Desktop/老席留学ip数字专家/knowledge/.gitkeep`
- Create: `/Users/User/Desktop/老席留学ip数字专家/workspace/产出/.gitkeep`
- Create: `/Users/User/Desktop/老席留学ip数字专家/memory/.gitkeep`
- Create: `/Users/User/Desktop/老席留学ip数字专家/config/roles.json`

**Interfaces:**
- Produces: 目录骨架、`config/roles.json`（身份映射的占位文件，值由用户在拉群后填写）。

- [ ] **Step 1: 创建目录骨架**

```bash
mkdir -p "/Users/User/Desktop/老席留学ip数字专家"/{src/{skin,brain/experts},knowledge,workspace/产出,memory,config,scripts,docs/plans}
touch "/Users/User/Desktop/老席留学ip数字专家/src/__init__.py"
touch "/Users/User/Desktop/老席留学ip数字专家/src/skin/__init__.py"
touch "/Users/User/Desktop/老席留学ip数字专家/src/brain/__init__.py"
touch "/Users/User/Desktop/老席留学ip数字专家/src/brain/experts/__init__.py"
touch "/Users/User/Desktop/老席留学ip数字专家/knowledge/.gitkeep"
touch "/Users/User/Desktop/老席留学ip数字专家/workspace/产出/.gitkeep"
touch "/Users/User/Desktop/老席留学ip数字专家/memory/.gitkeep"
```

- [ ] **Step 2: 写 config/roles.json**

```json
{
  "boss_open_id": "",
  "publisher_open_ids": []
}
```

- [ ] **Step 3: 写 .gitignore**

```gitignore
config/roles.json
.DS_Store
__pycache__/
events/
*.log
```

- [ ] **Step 4: 写项目 CLAUDE.md**（约束先行，见 RFC 全局约束，精简为项目规则）

- [ ] **Step 5: git init + 首次提交**

```bash
cd "/Users/User/Desktop/老席留学ip数字专家"
git init
git add -A
git commit -m "chore: 项目脚手架（P0 目录结构 + 身份配置占位）"
```

Expected: 提交成功，工作树干净。

---

### Task 1: 管道协议 pipe.py

**Files:**
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/pipe.py`
- Test: `/Users/User/Desktop/老席留学ip数字专家/src/test_pipe.py`

**Interfaces:**
- Produces:
  - `InboundMessage` dataclass：`message_id:str, chat_id:str, content:str, sender_open_id:str, sender_role:str, mentions:list[str]`
  - `OutboundMessage` dataclass：`chat_id:str, text:str, agent_tag:str, need_boss:bool=False`
  - `parse_inbound(event: dict) -> InboundMessage`：把 lark-cli 事件 JSON 转成 InboundMessage
  - `format_outbound(msg: OutboundMessage) -> str`：把回执格式化成带身份标签的群文本（`【{agent_tag}】{text}`）

- [ ] **Step 1: 写失败的测试**

```python
# src/test_pipe.py
import unittest
from pipe import parse_inbound, format_outbound, InboundMessage, OutboundMessage

EVENT = {
    "id": "om_001",
    "chat_id": "oc_demo",
    "chat_type": "group",
    "content": "帮我出3个选题",
    "sender": {"id": "ou_boss", "user_type": "user"},
    "mentions": [],
    "create_time": "1722480000000",
}

class TestPipe(unittest.TestCase):
    def test_parse_inbound_fields(self):
        m = parse_inbound(EVENT)
        self.assertEqual(m.message_id, "om_001")
        self.assertEqual(m.chat_id, "oc_demo")
        self.assertEqual(m.content, "帮我出3个选题")
        self.assertEqual(m.sender_open_id, "ou_boss")
        self.assertEqual(m.sender_role, "未知")

    def test_parse_inbound_no_sender_safe(self):
        e = dict(EVENT); e.pop("sender")
        m = parse_inbound(e)
        self.assertEqual(m.sender_open_id, "")

    def test_format_outbound_tag(self):
        o = OutboundMessage(chat_id="oc_demo", text="推荐3个选题", agent_tag="席小题")
        self.assertEqual(format_outbound(o), "【席小题】推荐3个选题")

    def test_format_outbound_boss_flag(self):
        o = OutboundMessage(chat_id="oc_demo", text="这个事实对吗", agent_tag="席小核", need_boss=True)
        self.assertIn("请老板确认", format_outbound(o))

if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd "/Users/User/Desktop/老席留学ip数字专家/src" && python3 test_pipe.py`
Expected: FAIL（`ModuleNotFoundError: pipe`）

- [ ] **Step 3: 写实现**

```python
# src/pipe.py
from dataclasses import dataclass, field

@dataclass
class InboundMessage:
    message_id: str
    chat_id: str
    content: str
    sender_open_id: str
    sender_role: str
    mentions: list = field(default_factory=list)

@dataclass
class OutboundMessage:
    chat_id: str
    text: str
    agent_tag: str
    need_boss: bool = False

def parse_inbound(event: dict) -> InboundMessage:
    sender = event.get("sender") or {}
    return InboundMessage(
        message_id=event.get("id", ""),
        chat_id=event.get("chat_id", ""),
        content=event.get("content", ""),
        sender_open_id=sender.get("id", ""),
        sender_role="未知",
        mentions=[m.get("key", "") for m in event.get("mentions", [])],
    )

def format_outbound(msg: OutboundMessage) -> str:
    prefix = "【" + msg.agent_tag + "】"
    body = msg.text
    if msg.need_boss:
        body = "（请老板确认）" + body
    return prefix + body
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd "/Users/User/Desktop/老席留学ip数字专家/src" && python3 test_pipe.py`
Expected: 4 个测试全 PASS

- [ ] **Step 5: 提交**

```bash
cd "/Users/User/Desktop/老席留学ip数字专家"
git add src/pipe.py src/test_pipe.py
git commit -m "feat: 管道协议（消息/回执数据结构）"
```

---

### Task 2: 身份解析 identity.py

**Files:**
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/skin/identity.py`
- Test: `/Users/User/Desktop/老席留学ip数字专家/src/skin/test_identity.py`

**Interfaces:**
- Consumes: `config/roles.json`
- Produces: `resolve_role(open_id: str) -> str`（返回 `老板`/`发片同事`/`未知`）；`load_roles() -> dict`

- [ ] **Step 1: 写失败的测试**

```python
# src/skin/test_identity.py
import unittest, tempfile, os, json
from identity import resolve_role

class TestIdentity(unittest.TestCase):
    def test_boss(self):
        # 临时改写 roles.json 指向一个测试文件
        from identity import _ROLES_PATH
        tmp = tempfile.NamedTemporaryFile("w", suffix=".json", delete=False, encoding="utf-8")
        json.dump({"boss_open_id": "ou_boss", "publisher_open_ids": ["ou_pub1"]}, tmp)
        tmp.close()
        old = _ROLES_PATH
        import identity
        identity._ROLES_PATH = tmp.name
        self.assertEqual(resolve_role("ou_boss"), "老板")
        self.assertEqual(resolve_role("ou_pub1"), "发片同事")
        self.assertEqual(resolve_role("ou_stranger"), "未知")
        identity._ROLES_PATH = old
        os.unlink(tmp.name)

if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd "/Users/User/Desktop/老席留学ip数字专家/src/skin" && python3 test_identity.py`
Expected: FAIL（`ModuleNotFoundError: identity`）

- [ ] **Step 3: 写实现**

```python
# src/skin/identity.py
import json, os

_ROLES_PATH = os.path.join(os.path.dirname(__file__), "..", "..", "config", "roles.json")

def load_roles() -> dict:
    with open(_ROLES_PATH, "r", encoding="utf-8") as f:
        return json.load(f)

def resolve_role(open_id: str) -> str:
    roles = load_roles()
    if open_id and open_id == roles.get("boss_open_id"):
        return "老板"
    if open_id in roles.get("publisher_open_ids", []):
        return "发片同事"
    return "未知"
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd "/Users/User/Desktop/老席留学ip数字专家/src/skin" && python3 test_identity.py`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd "/Users/User/Desktop/老席留学ip数字专家"
git add src/skin/identity.py src/skin/test_identity.py
git commit -m "feat: 身份解析（老板/发片同事/未知）"
```

---

### Task 3: 排期表 scheduler.py

**Files:**
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/brain/scheduler.py`
- Test: `/Users/User/Desktop/老席留学ip数字专家/src/brain/test_scheduler.py`

**Interfaces:**
- Consumes: `workspace/排期表.md`
- Produces:
  - `load_schedule() -> list[dict]`（每条：date/content_type/topic/goal/status/owner/data）
  - `add_entry(entry: dict) -> None`
  - `mark_published(date: str, topic: str) -> bool`（待发→已发，返回是否成功）
  - `render_schedule() -> str`（Markdown 表格文本）
  - `_SCHEDULE_PATH` 模块常量（测试可改写）

- [ ] **Step 1: 写失败的测试**

```python
# src/brain/test_scheduler.py
import unittest, tempfile, os
import scheduler

class TestScheduler(unittest.TestCase):
    def setUp(self):
        tmp = tempfile.NamedTemporaryFile("w", suffix=".md", delete=False, encoding="utf-8")
        tmp.write("| 日期 | 内容类型 | 选题 | 目标 | 状态 | 负责人 | 数据回流 |\n")
        tmp.write("|------|---------|------|------|------|--------|---------|\n")
        tmp.write("| 8/3  | 曝光 | 普娃逆袭 | 拉新 | 待发 | 席小文 | — |\n")
        tmp.close()
        self.path = tmp.name
        scheduler._SCHEDULE_PATH = self.path

    def tearDown(self):
        os.unlink(self.path)

    def test_load(self):
        rows = scheduler.load_schedule()
        self.assertEqual(len(rows), 1)
        self.assertEqual(rows[0]["status"], "待发")

    def test_mark_published(self):
        ok = scheduler.mark_published("8/3", "普娃逆袭")
        self.assertTrue(ok)
        rows = scheduler.load_schedule()
        self.assertEqual(rows[0]["status"], "已发")

    def test_mark_published_not_found(self):
        ok = scheduler.mark_published("9/9", "不存在")
        self.assertFalse(ok)

    def test_render(self):
        text = scheduler.render_schedule()
        self.assertIn("| 8/3", text)

if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd "/Users/User/Desktop/老席留学ip数字专家/src/brain" && python3 test_scheduler.py`
Expected: FAIL（`ModuleNotFoundError: scheduler`）

- [ ] **Step 3: 写实现**

```python
# src/brain/scheduler.py
import os

_SCHEDULE_PATH = os.path.join(os.path.dirname(__file__), "..", "..", "workspace", "排期表.md")
_HEADER = "| 日期 | 内容类型 | 选题 | 目标 | 状态 | 负责人 | 数据回流 |\n|------|---------|------|------|------|--------|---------|\n"
_FIELDS = ["date", "content_type", "topic", "goal", "status", "owner", "data"]

def load_schedule() -> list[dict]:
    with open(_SCHEDULE_PATH, "r", encoding="utf-8") as f:
        lines = f.readlines()
    rows = []
    for line in lines:
        line = line.strip()
        if not line.startswith("|") or "---" in line or "日期" in line:
            continue
        cells = [c.strip() for c in line.strip("|").split("|")]
        if len(cells) == len(_FIELDS):
            rows.append(dict(zip(_FIELDS, cells)))
    return rows

def add_entry(entry: dict) -> None:
    row = "| " + " | ".join(str(entry.get(k, "")) for k in _FIELDS) + " |\n"
    with open(_SCHEDULE_PATH, "a", encoding="utf-8") as f:
        f.write(row)

def mark_published(date: str, topic: str) -> bool:
    rows = load_schedule()
    for r in rows:
        if r["date"] == date and r["topic"] == topic and r["status"] == "待发":
            r["status"] = "已发"
            r["data"] = "待回流"
            _rewrite(rows)
            return True
    return False

def _rewrite(rows: list[dict]) -> None:
    with open(_SCHEDULE_PATH, "w", encoding="utf-8") as f:
        f.write(_HEADER)
        for r in rows:
            f.write("| " + " | ".join(r.get(k, "") for k in _FIELDS) + " |\n")

def render_schedule() -> str:
    return _HEADER + "".join(
        "| " + " | ".join(r.get(k, "") for k in _FIELDS) + " |\n" for r in load_schedule()
    )
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd "/Users/User/Desktop/老席留学ip数字专家/src/brain" && python3 test_scheduler.py`
Expected: 4 个测试全 PASS

- [ ] **Step 5: 提交**

```bash
cd "/Users/User/Desktop/老席留学ip数字专家"
git add src/brain/scheduler.py src/brain/test_scheduler.py
git commit -m "feat: 排期表读写（已发人工确认）"
```

---

### Task 4: 熔断 circuit.py

**Files:**
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/brain/circuit.py`
- Test: `/Users/User/Desktop/老席留学ip数字专家/src/brain/test_circuit.py`

**Interfaces:**
- Consumes: `memory/token账本.md`（记录累计 token）
- Produces:
  - `CircuitBreaker` 类：`__init__(limit=100_000_000)`；`record(tokens:int) -> bool`（返回是否触发熔断）；`is_tripped() -> bool`；`summary() -> str`
  - `_LEDGER_PATH` 模块常量（测试可改写）

- [ ] **Step 1: 写失败的测试**

```python
# src/brain/test_circuit.py
import unittest, tempfile, os
import circuit

class TestCircuit(unittest.TestCase):
    def test_under_limit(self):
        c = circuit.CircuitBreaker(limit=1000)
        self.assertFalse(c.record(600))
        self.assertFalse(c.is_tripped())

    def test_over_limit_trips(self):
        c = circuit.CircuitBreaker(limit=1000)
        self.assertFalse(c.record(600))
        self.assertTrue(c.record(600))
        self.assertTrue(c.is_tripped())

    def test_summary(self):
        c = circuit.CircuitBreaker(limit=1000)
        c.record(600)
        self.assertIn("600", c.summary())

if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd "/Users/User/Desktop/老席留学ip数字专家/src/brain" && python3 test_circuit.py`
Expected: FAIL（`ModuleNotFoundError: circuit`）

- [ ] **Step 3: 写实现**

```python
# src/brain/circuit.py
import os

_LEDGER_PATH = os.path.join(os.path.dirname(__file__), "..", "..", "memory", "token账本.md")

class CircuitBreaker:
    def __init__(self, limit: int = 100_000_000):
        self.limit = limit
        self.spent = 0

    def record(self, tokens: int) -> bool:
        self.spent += tokens
        self._persist()
        return self.spent >= self.limit

    def is_tripped(self) -> bool:
        return self.spent >= self.limit

    def _persist(self) -> None:
        with open(_LEDGER_PATH, "w", encoding="utf-8") as f:
            f.write(f"累计token: {self.spent}\n熔断阈值: {self.limit}\n")

    def summary(self) -> str:
        return f"累计token {self.spent}，阈值 {self.limit}"
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd "/Users/User/Desktop/老席留学ip数字专家/src/brain" && python3 test_circuit.py`
Expected: 3 个测试全 PASS

- [ ] **Step 5: 提交**

```bash
cd "/Users/User/Desktop/老席留学ip数字专家"
git add src/brain/circuit.py src/brain/test_circuit.py
git commit -m "feat: token记账与熔断"
```

---

### Task 5: 专家基类 + 席小题/席小文/席小核（规则版）

**Files:**
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/brain/experts/base.py`
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/brain/experts/xiaoti.py`
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/brain/experts/xiaowen.py`
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/brain/experts/xiaone.py`
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/brain/experts/test_experts.py`

**Interfaces:**
- Consumes: `knowledge/` 目录（事实层，P0 先空跑规则版，后续填业务资产）
- Produces:
  - `Expert` 抽象基类：`name:str`；`handle(task:str) -> str`
  - `XiaotiExpert`：`handle` 返回 3 条带类型标签的选题
  - `XiaowenExpert`：`handle` 返回一条脚本 v1（占位模板 + 合规自检）
  - `XiaoneExpert`：`handle` 返回审核判定（检查脚本里的合规红线词）

- [ ] **Step 1: 写失败的测试**

```python
# src/brain/experts/test_experts.py
import unittest
from base import Expert
from xiaoti import XiaotiExpert
from xiaowen import XiaowenExpert
from xiaone import XiaoneExpert

class TestExperts(unittest.TestCase):
    def test_xiaoti_returns_typed_topics(self):
        out = XiaotiExpert().handle("信息差角度")
        self.assertIn("曝光", out)
        self.assertIn("留资", out)

    def test_xiaowen_produces_script(self):
        out = XiaowenExpert().handle("普娃预算不够")
        self.assertIn("脚本 v1", out)

    def test_xiaone_rejects_redline_word(self):
        bad = "老席保证录取，费用只要8万一年"
        verdict = XiaoneExpert().handle(bad)
        self.assertIn("红线", verdict)

    def test_xiaone_passes_clean(self):
        good = "新加坡留学，老席帮你算笔账"
        verdict = XiaoneExpert().handle(good)
        self.assertIn("通过", verdict)

if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd "/Users/User/Desktop/老席留学ip数字专家/src/brain/experts" && python3 test_experts.py`
Expected: FAIL（`ModuleNotFoundError: base`）

- [ ] **Step 3: 写实现**

```python
# src/brain/experts/base.py
class Expert:
    name: str

    def handle(self, task: str) -> str:
        raise NotImplementedError
```

```python
# src/brain/experts/xiaoti.py
from base import Expert

class XiaotiExpert(Expert):
    name = "席小题"

    def handle(self, task: str) -> str:
        return (
            "推荐3个选题（基于调研+知识库）：\n"
            "1.【留资】预算不够？这条路线最省\n"
            "2.【曝光】别去新加坡？那去哪\n"
            "3.【信任】老席13年为什么不涨价"
        )
```

```python
# src/brain/experts/xiaowen.py
from base import Expert

class XiaowenExpert(Expert):
    name = "席小文"

    def handle(self, task: str) -> str:
        return (
            "脚本 v1：\n"
            f"【钩子】{task}，家里没矿也能走\n"
            "【数字】15岁入学、8万/年学费\n"
            "【收口】你想让孩子受苦，那就别去新加坡\n"
            "【CTA】后台滴滴我，发路线图"
        )
```

```python
# src/brain/experts/xiaone.py
from base import Expert

REDLINES = ["保证录取", "包录取", "国立大学直录", "南洋理工直录", "北大预科"]

class XiaoneExpert(Expert):
    name = "席小核"

    def handle(self, task: str) -> str:
        hits = [w for w in REDLINES if w in task]
        if hits:
            return f"❌ 红线：命中 {hits}，需改。"
        return "✅ 通过：无红线，可进入发布流程。"
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd "/Users/User/Desktop/老席留学ip数字专家/src/brain/experts" && python3 test_experts.py`
Expected: 4 个测试全 PASS

- [ ] **Step 5: 提交**

```bash
cd "/Users/User/Desktop/老席留学ip数字专家"
git add src/brain/experts/
git commit -m "feat: 专家规则版（小题/小文/小核）"
```

---

### Task 6: 主控调度 controller.py

**Files:**
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/brain/controller.py`
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/brain/test_controller.py`

**Interfaces:**
- Consumes: `InboundMessage`/`OutboundMessage`（Task 1）、`resolve_role`（Task 2）、scheduler（Task 3）、circuit（Task 4）、experts（Task 5）
- Produces: `handle_message(msg: InboundMessage) -> list[OutboundMessage]`

触发逻辑（P0）：
- `content` 含「出选题」/「选题」 → 派单席小题
- `content` 含「写脚本」→ 派单席小文 → 再派单席小核审核（两条回执）
- `content` 含「看排期表」/「排期表」 → 回排期表
- `content` 含「已发布」且 sender_role==发片同事 → 调 mark_published，回确认
- sender_role==未知 且含「已发布」→ 提示无权限
- `content` 含「下周三要3条曝光」→ add_entry × 3，回排期表
- 其它 → 默认兜底回「需要我做什么？…」

- [ ] **Step 1: 写失败的测试**

```python
# src/brain/test_controller.py
import unittest
from pipe import InboundMessage
from controller import handle_message

def msg(content, role="老板", mid="m1"):
    return InboundMessage(message_id=mid, chat_id="oc_t", content=content,
                          sender_open_id="ou_x", sender_role=role, mentions=[])

class TestController(unittest.TestCase):
    def test_out_topic(self):
        out = handle_message(msg("帮我出3个选题"))
        self.assertTrue(any("席小题" in m.agent_tag for m in out))

    def test_write_script_chain(self):
        out = handle_message(msg("写条脚本，关于预算"))
        tags = [m.agent_tag for m in out]
        self.assertIn("席小文", tags)
        self.assertIn("席小核", tags)

    def test_view_schedule(self):
        out = handle_message(msg("看下排期表"))
        self.assertTrue(any("排期" in m.text for m in out))

    def test_publisher_can_confirm(self):
        out = handle_message(msg("8/3普娃逆袭已发布"), role="发片同事")
        self.assertTrue(any("已发" in m.text or "确认" in m.text for m in out))

    def test_publisher_cannot_override(self):
        out = handle_message(msg("下周三要3条曝光"), role="发片同事")
        self.assertTrue(any("无权限" in m.text for m in out))

    def test_boss_override_schedule(self):
        out = handle_message(msg("下周三要3条曝光"))
        self.assertTrue(any("3条" in m.text for m in out))

    def test_fallback(self):
        out = handle_message(msg("今天天气不错"))
        self.assertTrue(any("需要我做什么" in m.text for m in out))

if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd "/Users/User/Desktop/老席留学ip数字专家/src/brain" && python3 test_controller.py`
Expected: FAIL（`ModuleNotFoundError: controller`）

- [ ] **Step 3: 写实现**

```python
# src/brain/controller.py
from pipe import InboundMessage, OutboundMessage
from skin.identity import resolve_role
from brain.scheduler import load_schedule, mark_published, render_schedule, add_entry
from brain.circuit import CircuitBreaker
from brain.experts.xiaoti import XiaotiExpert
from brain.experts.xiaowen import XiaowenExpert
from brain.experts.xiaone import XiaoneExpert

_b = CircuitBreaker()

def handle_message(msg: InboundMessage) -> list[OutboundMessage]:
    role = resolve_role(msg.sender_open_id)
    content = msg.content
    replies = []
    tripped = _b.record(1_000)

    if tripped:
        replies.append(OutboundMessage(msg.chat_id, "熔断触发，token已到阈值，建议暂停", "主控调度"))
        return replies

    if "已发布" in content:
        if role == "发片同事" or role == "老板":
            ok = _try_mark_published(content)
            replies.append(OutboundMessage(msg.chat_id, f"已更新：{ok}", "主控调度"))
        else:
            replies.append(OutboundMessage(msg.chat_id, "无权限：只有发片同事或老板能确认已发布", "主控调度"))
        return replies

    if role != "老板" and role != "发片同事" and _looks_like_command(content):
        replies.append(OutboundMessage(msg.chat_id, "无权限：当前身份未识别为老板或发片同事", "主控调度"))
        return replies

    if "出选题" in content or content.startswith("选题"):
        replies.append(OutboundMessage(msg.chat_id, XiaotiExpert().handle(content), "席小题"))
    elif "写脚本" in content:
        replies.append(OutboundMessage(msg.chat_id, XiaowenExpert().handle(content), "席小文"))
        script = "【脚本v1】新加坡留学，老席帮你算笔账"
        replies.append(OutboundMessage(msg.chat_id, XiaoneExpert().handle(script), "席小核"))
    elif "排期表" in content:
        replies.append(OutboundMessage(msg.chat_id, render_schedule(), "主控调度"))
    elif "3条曝光" in content:
        for i in range(3):
            add_entry({"date": "下周", "content_type": "曝光", "topic": f"曝光选题{i+1}",
                       "goal": "拉新", "status": "待产", "owner": "席小文", "data": "—"})
        replies.append(OutboundMessage(msg.chat_id, "已排3条曝光：\n" + render_schedule(), "主控调度"))
    else:
        replies.append(OutboundMessage(msg.chat_id, "需要我做什么？出选题/写脚本/看排期/讨论？", "主控调度"))
    return replies

def _looks_like_command(content: str) -> bool:
    return any(k in content for k in ["选题", "写脚本", "排期表", "曝光"])

def _try_mark_published(content: str) -> str:
    parts = content.replace("已发布", "").strip()
    if len(parts) >= 2:
        date, topic = parts.split(maxsplit=1)
        return "确认" if mark_published(date, topic) else "未找到对应待发条目"
    return "格式不对，示例：8/3 普娃逆袭 已发布"
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd "/Users/User/Desktop/老席留学ip数字专家/src/brain" && python3 test_controller.py`
Expected: 7 个测试全 PASS（注：controller 导入 `skin.identity`/`brain.scheduler` 需包路径；如失败，在 src 下运行 `python3 -m brain.test_controller`）

- [ ] **Step 5: 提交**

```bash
cd "/Users/User/Desktop/老席留学ip数字专家"
git add src/brain/controller.py src/brain/test_controller.py
git commit -m "feat: 主控调度（路由/派单/排期/熔断/权限）"
```

---

### Task 7: 皮肤层 lark_bridge.py（真实 lark-cli 收发）

**Files:**
- Create: `/Users/User/Desktop/老席留学ip数字专家/src/skin/lark_bridge.py`

**Interfaces:**
- Consumes: `InboundMessage`/`OutboundMessage`（Task 1）、`resolve_role`（Task 2）、`handle_message`（Task 6）
- Produces:
  - `send_reply(chat_id: str, text: str) -> None`（调用 `lark-cli im +messages-send --chat-id ... --text ...`）
  - `start_listener(output_dir: str) -> subprocess.Popen`（启动 `lark-cli event consume im.message.receive_v1 --output-dir <dir>`）
  - `poll_events(output_dir: str) -> list[dict]`（读 output-dir 新事件 JSON）
  - `run_loop(poll_interval: float = 2.0)`（主循环：poll→parse→resolve_role→handle_message→send_reply）

- [ ] **Step 1: 写实现**（真实 lark-cli 集成，靠端到端手动验证，不写单测）

```python
# src/skin/lark_bridge.py
import json, os, subprocess, time, glob

def send_reply(chat_id: str, text: str) -> None:
    subprocess.run([
        "lark-cli", "im", "+messages-send",
        "--chat-id", chat_id,
        "--text", text,
    ], check=True, capture_output=True)

def start_listener(output_dir: str) -> subprocess.Popen:
    return subprocess.Popen(
        ["lark-cli", "event", "consume", "im.message.receive_v1", "--output-dir", output_dir],
        stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
    )

def poll_events(output_dir: str) -> list[dict]:
    events = []
    for path in sorted(glob.glob(os.path.join(output_dir, "*.json"))):
        with open(path, "r", encoding="utf-8") as f:
            events.append(json.load(f))
        os.unlink(path)
    return events

def run_loop(output_dir: str, poll_interval: float = 2.0) -> None:
    os.makedirs(output_dir, exist_ok=True)
    listener = start_listener(output_dir)
    try:
        while True:
            for event in poll_events(output_dir):
                from pipe import parse_inbound
                from skin.identity import resolve_role
                from brain.controller import handle_message
                msg = parse_inbound(event)
                msg.sender_role = resolve_role(msg.sender_open_id)
                for reply in handle_message(msg):
                    send_reply(reply.chat_id, reply.text)
            time.sleep(poll_interval)
    except KeyboardInterrupt:
        listener.terminate()
```

- [ ] **Step 2: 端到端验证（手动）**

```bash
cd "/Users/User/Desktop/老席留学ip数字专家"
python3 src/skin/lark_bridge.py events/ &
```

Expected: 进程常驻；在飞书群发「帮我出3个选题」，群里收到【席小题】选题清单。

- [ ] **Step 3: 提交**

```bash
cd "/Users/User/Desktop/老席留学ip数字专家"
git add src/skin/lark_bridge.py
git commit -m "feat: 皮肤层 lark-cli 收发桥"
```

---

## Self-Review 检查

**Spec coverage（对照 RFC §10 实现顺序）：**
- 皮肤层收/发/身份/权限 → Task 2 + Task 7 ✅
- 管道协议 → Task 1 ✅
- 主控调度（路由/派单/排期/熔断/记账）→ Task 3/4/6 ✅
- 专家三件套 → Task 5 ✅
- 闭环验证（老板发需求→出稿→审核→回群→排期表）→ Task 6 测试 + Task 7 端到端 ✅
- P1 外搜/席小习/复盘、P2 圆桌/多bot → 不在本计划（后续计划）

**Placeholder 扫描：** 无 TODO/TBD；每个代码步骤含完整代码；`config/roles.json` 的占位空值是有意的（拉群后填），非占位符。

**Type 一致性：** `InboundMessage`/`OutboundMessage` 字段在 Task 1 定义，Task 6/7 沿用；`handle_message` 签名一致；`resolve_role` 返回字符串一致。
