# 变更记录 2026-08-11

---

## 15:40 · basis.py（新建）+ controller.py + xiaoxi.py + test_controller.py · 「选题四的依据」只回那一条 + 席小习拆解拍板答案落库

**老板反馈**（测试群 14:26，bad case「选题四的依据回全部」；能力疑问：「我用我自己的语言修改脚本时候，席小习有拆解并学习到的能力么，还要能对比到哪里有不同」）。

**判断更正**：上一轮误报「多 bot 模式下席小习学习落库断链」——实际**是通的**：席小习在**自己的进程**里 `_run_expert → run_expert → learn_rules_flow → rules.add_rule` 落「待确认」，老板回「确认」→ `confirm_pending` 落「已确认」。真正病灶有两处：① `_resolve_doubt`（wait_boss 拍板路径）给实质答案时只 `add_confirmed_rule` 落一条「答案原文」，**席小习完全不参与拆解**；② `_revise` 路径席小习对比素材不足，且 prompt 没锁「对比哪里不同」的输出契约。

**改动**：
- `src/brain/basis.py`（新建）：公共序号解析模块——`match_index`（「第N条/选题四」→ int）、`numbered_topics`、`basis_map`（兼容「N. 依据」单行 +「（选题N依据…）块状」）、`paired_topics`、`item_reply`。从 skin 层 multi_main 迁出逻辑，多 bot 暂不动皮肤层（降回归风险）。
- `controller.py` `_ask`（1128 起）：speaker==席小题 且有 basis 时先 `basis_util.match_index(content)`——命中序号 → 只回 `选题{n}「{题目}」\n依据：{reason}`（复用 multi_main `_match_basis_question` 回执格式）；没找到该序号 → 明说「没找到第 N 个选题的依据」；无序号 → 仍回全部依据（兜底不丢功能）。
- `controller.py` `_resolve_doubt`（1339 起）：老板拍板实质答案时，在 `add_confirmed_rule` 后追加**同步**席小习拆解（复用 `_exec_revise_review_rules` 单 bot 同步模式，不走 `_emit` 派单、不加二次确认）——`_controller_factories()["席小习"]().handle(f"{answer}\n把老板这段确定答案拆解成审核/写作规则，直接给 JSON。", context=ctx)` → `parse_learned_rules` → `rules.add_confirmed_rule`。失败只 `_log.error`，不阻塞主流程。
- `experts/xiaoxi.py` prompt：行为规则段加两条——完整改后稿 → 先逐点列差异（AI原版 X → 老板改后 Y）再提炼规则；只一句修改要求 → 直接提炼规则并明说「给不出逐句对比」，不编造 diff。

**测试**：test_controller 新增 3 条——`test_ask_basis_picks_nth`（选题四 → 只回第 4 条不含 1/2 依据）、`test_ask_basis_fallback_all_when_no_index`（无序号仍回全部）、`test_resolve_doubt_learns_via_xiaoxi`（拍板答案 → 席小习拆解规则落「已确认」）。181→184 全绿；scheduler 17 / intent 45 / session 6 / experts 34 回归全绿。

**运维**：小席（controller.py + basis.py）、席小习（xiaoxi.py prompt）整树重启，改动生效。注意：`_last_passed` 是模块态，重启后清空，重测要么带标题/日期，要么重跑一条快流水线。
