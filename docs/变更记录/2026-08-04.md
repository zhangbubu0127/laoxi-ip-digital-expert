# 变更记录 2026-08-04

## 09:38 · docs/需求与决策记录.md · 补 §2.20 RAG 触发条件（基于实测）：全库 42 万字符≈10 万 token、任务子集化后单次注入仅几千–2 万 token，当前不需要 RAG；三个触发信号（①单篇子集>3万 token，现最大单篇爆款分析库 6.9 万字符≈2 万 token 先做分层/切片 ②单任务跨 3+ 分类拼接 ③load_qna 关键词兜底合并成常态>30%）；量化阈值（全库涨到 ~30 万 token 且子集化失效、或单次任务注入>模型上下文窗口 50% 才上向量检索）；修正 §2.20 标题错位（内容原误标「七、待补设计」落在 §2.22 后，归位到 §2.19 与 §2.21 之间并删重复标题）；旧版归档 docs/archive/需求与决策记录.md-20260804-0938.md

## 11:10 · src/brain/intent.py + src/brain/controller.py · 「说安排变真派单」（老板 10:44 群聊「根据这个出内容吧」被识别成「其他」，主控只空头承诺没派单）：①intent.py 写脚本识别增强——LLM 提示词「写脚本」定义补「出内容/按这个出/根据这个出」变体；by_keyword 新增 WRITE_SCRIPT_HINTS（写条/出条/出内容/做内容/出个60秒/文案/台本/脚本 等），「根据这个出内容吧」归正为「写脚本」；②controller.py 抽取 _write_script() 复用写脚本流水线（席小文写作→席小核审核），新增 _force_script_task()——意图落「其他」但命中产出指令且有上一条产出可依据时，强制走写脚本派单，杜绝只承诺不执行；③test_intent + test_controller 补用例（出内容变体→写脚本；意图「其他」+产出指令→仍派单席小文；无上一条产出时回落闲聊），全量测试绿

## 11:45 · src/brain/experts/xiaone.py + xiaowen.py + dispatch.py + intent.py + controller.py + context_store.py + multi_main.py · 审核体验四联修（老板一次提 4 个问题，查根因后全修）：
①**问题1 写脚本连续性**——根因：主控把「第N个写条脚本」直接传席小文，无选题列表约束，写手自由发挥。修复：controller `_resolve_script_task()` 把「第N个」解析成上一条席小题产出的具体选题文字；xiaowen.handle 加硬性要求「任务给了选题/选题列表必须严格写指定的那条，禁止发明替换泛化」。
②**问题2 审核不刷屏**——根因：脚本被拼进 @派单 task 全文发群。修复：派单任务只写「审核这条脚本」，脚本原文走 context_store 上下文；xiaone.handle 红线扫描连 task+context 一起扫；multi_main 席小核分支 split_review → 只发公开段。
③**问题3 审核结论精简+问理由**——根因：席小核输出含 JSON 英文术语键（judgement/claims）+ 冗长，全发群。修复：席小核改双段输出协议（公开段=【审核结论】通过/不通过/需老板确认 + 疑问段 + 「需要给您展示理由吗？」；`===审核详情===` 后详情段中文白话剥 JSON，存 `context_store.save_review`）；controller `_show_review_reasons`（「要/看看」）+ `_ask` 席小核分支（追问理由）直发 `load_review` 详情，不重新生成；dispatch 补席小核 explain 路由。
④**问题4 审核误判 bug**——根因：主控 `_verdict_needs_change` 子串匹配「红线」，真实输出「通过：无红线」被误判需改 → 误发重写指令。修复：改三态判定 `_verdict_classify`（通过/不通过/需老板确认），只认明确拒稿信号（不通过/未通过/❌/需改）与疑问信号（需老板确认/待确认），绝不扫「红线」词面。
⑤**顶层架构提示词补充**（老板要求）：intent.py 主控调度 prompt 加「协同协议」——写脚本走席小文出稿→席小核审核，席小核结论只发三选一+疑问+问理由，脚本不刷屏走后台，老板要理由才展示详情。
⑥**验证**：新增用例 controller 4（三态判定回归、第N个解析、追问席小核直发理由、其他意图「要」直发理由）、experts 4（红线连 context 扫、split_review、剥 JSON、fail_reply 双段）、context_store 2（review 存读、take 忽略 review 文件）；58+22+9+25 全绿。旧版 架构书.md/需求与决策记录.md 已归档 docs/archive/。

## 14:25 · src/skin/multi_main.py + src/brain/controller.py · 席小题选题依据布局优化 + 席小文超时修复（老板反馈：依据回复观感差 + 席小文生成脚本超时未回复）：
①**布局优化**——根因：原来「选题N『标题』依据：理由」全糊在一行，多个选题之间也没分隔。修复：multi_main `_match_basis_question` 重写——按题配对（`_paired_topics` 用序号对齐选题/依据），每个选题「选题N「题目」\n依据：理由」分行，块与块之间空行分隔；支持「第N个」指代（`_match_index` 数字+中文数字）；test_multi_main 补布局用例。
②**超时根因**（日志证据链 14:14:54 finish=length、completion_tokens=8000、reasoning_tokens=8000 空输出 → 14:18:22 重试 15997 仍空 → 单线程阻塞）：(1) 意图识别把「选题三给我出两条脚本看看」的选题指代没解析（controller `_resolve_script_task` 原只认「第N个/条」），写手拿到泛化任务自己编了选题；(2) 席小文 prompt 注入知识库 1 万+ token，推理模型 deepseek-v4-flash 把默认 max_tokens=8000 全花在 reasoning 上→正文空；(3) llm.py 空输出重试用同一 prompt 反复试、预算越高 reasoning 吃越多，越陷越深。
③**修复**——controller `_resolve_script_task`/`_topic_by_index`/`_clean_topic`：识别「选题N」指代，优先读席小题依据存档（load_basis）的选题列表，回落会话记忆上一条产出，解析成具体选题文字再派单；xiaowen/xiaone handle 默认 max_tokens 8000→12000 防空；llm.py 空输出重试改为带「直接输出正文、不再推理」提示 + 2 次限时（timeout 120）+ 预算步进 4000，打断 reasoning 死循环、卡死时长减半。
④**验证**：FakeGen 加 `**kwargs`（max_tokens 传参）；controller 补「选题三 via 依据存档」用例；全量测试绿（controller 59 + experts 22 + multi_main 10 + 其余各套）。

## 14:40 · src/brain/controller.py · 补审核「流水线超时后迟到脚本不@席小核」漏洞（老板问：小席说了安排席小核审核为啥不@他）：
①**根因**（日志证据链）：14:13:03 派单席小文+席小核、14:13:05 小席发「写脚本进行中…」；席小文因上一轮修的 DeepSeek 空输出螺旋实际耗时 7.7 分钟（14:13→14:20:48）；`_PIPE_TIMEOUT=180s`，流水线 14:19:26 被 `sweep_pipelines` 清掉并发「流水线超时」；14:20:48/14:21:56 席小文迟到脚本到达时 `_pipelines[chat]` 已不存在 → `handle_bot_output` 直接返回，主控不再补派席小核 → 老板只看到「说要审核」+ 脚本，却等不到审核。
②**修复**——`handle_bot_output` 无流水线分支补恢复逻辑：识别席小文脚本形输出（`_is_script_output`：以【席小文】开头且含「## 脚本」）→ 仍补派席小核（`审核这条脚本`+脚本原文），多bot 模式重挂 wait_he 流水线（继续走三态判定/详情存档/自动修改），单bot 兜底进程内直接审核；非脚本形输出（闲聊/追问回复）不触发。
③**验证**：controller 补 3 用例（超时后多bot 补派@席小核+重挂 wait_he、超时后单bot 进程内审核、非脚本输出不触发）；全量测试绿（controller 62 + 其余各套）。

## 15:00 · src/brain/intent.py + controller.py + experts/xiaowen.py + xiaone.py · 「审核」意图落地 + 提示词反过度推理（老板问：审核一下为啥只聊天不派单 + 小文推理预算不够要不要继续加）：
①**「审核一下」只聊天不派单**——根因：意图列表里没有「审核」意图，14:26:58「审核一下」/14:27:33「刚刚生成的文案，审核以西」被 LLM 判成「其他」，小席只回「马上安排席小核审核」却未派单。修复：intent.py 新增「审核脚本」意图（INTENTS + LLM 提示词描述 + `_REVIEW_HINTS` 关键词兜底：审核/审一下/审审/复审/过一遍/把把关，置于写脚本/出选题匹配之前，防「文案/脚本/选题」误命中）；controller 新增 `_review_script` + `_last_script`（会话记忆里找最近一条席小文脚本产出）→ 派席小核「审核这条脚本」+脚本原文，多bot @ / 单bot `_post_xiaone` 双段（公开+存档详情），无脚本则追问「要审核哪条」；「审核脚本」加入 _ADMIN_ONLY。
②**推理预算要不要继续加**——结论：不加（证据：deepseek-v4-flash 是推理模型，reasoning 会填满给定预算，8000→全 reasoning、16000→15997 reasoning，加预算只是让它想更久且卡更久）。修复：不改 max_tokens，改在源头压 reasoning——xiaowen/xiaone 系统提示各加「输出任务，直接给结果，不要展开长篇推理过程」；配合已有 nudge 空输出重试（实测 reasoning 800→266、15s 出结果）。
③**验证**：test_intent +2（LLM 识别/关键词兜底 4 变体）、test_controller +3（单bot 派席小核审上一条脚本、无脚本追问、多bot @席小核）、test_experts +2（两专家 prompt 含反过度推理）；全量测试绿（intent 27 + controller 65 + experts 24 + 其余各套）。

## 15:45 · src/brain/experts/xiaone.py + controller.py + test_controller.py + test_experts.py · 「展示理由」无法显示修复（老板 14:51 问：让席小核展示审理理由显示不了）：
①**根因**（日志证据链 14:50:56-57）：席小核审核 LLM 调用 completion_tokens=5962 里 reasoning_tokens=5631（94% 被推理吃掉），正文只吐出 21 字符「【审核结论】：通过\n\n需要给您展示理由吗？」——**没有 `===审核详情===` 段**；`split_review` 详情为空 → 未存档 → 老板「展示理由」（意图=追问→`_ask` 席小核分支）读 `load_review` 空 → 死回「没存详情」。注：此失败发生在 15:00 反过度推理 prompt 上线前，但该修复不保证详情段被写出，「要理由」仍可能吃闭门羹。
②**修复**——xiaone.py：把【审核详情】段标为「必须交付的存档内容，不是可省的推理过程」，无论通过/不通过都要写 2-4 条判定依据；行为规则 4 改为「不要展示内心推理草稿，直接给结论；但【审核详情】段是交付内容的一部分，必须写完」（保留反推理、强制详情段）；controller.py：新增 `_review_reasons()`——有存档直发详情，存档缺失（模型省略详情段/存档超时被清）时用席小核 `explain()` 基于上一条审核结论现生成简洁理由，`_ask` 席小核分支与 `_show_review_reasons`（其他意图「要/看看」）都走它，不再死回「没存详情」。
③**验证**：test_controller +2（追问无存档走 explain 兜底、其他意图「要」无存档走 explain 兜底）、test_experts +1（详情段「必须交付/必须写完」prompt 断言）+ 修正反推理断言措辞；全量测试绿（controller 67 + experts 25 + 其余各套）。

## 16:20 · src/brain/controller.py + rules.py + test_controller.py + test_rules.py · 审核疑问点→暂停@老板→学习→修改→复审（老板需求：席小核给的是疑问点而非确定红线时，先暂停并@用户拿确定答案，学完再让席小文改）：
①**问题**——原 doubt 分支（席小核判「需老板确认」）只弹一句「有疑问需您确认，已暂停自动流程」就 `_pipelines.pop` 丢掉流水线：不@老板、老板答复后无闭环（不学习/不修改/不复审），疑问悬空。
②**修复**——controller.py：新增 `_admin_at_mentions()`（读 roles.json boss+product open_id，群内@拍板，boss 未配时用产品）；doubt 分支改挂 `wait_boss` 状态 + @老板展示疑问点请给确定答案；`handle_message` 拦截——wait_boss 挂起且发送者是 admin 时，消息即视为老板答复走 `_resolve_doubt`（放弃词「算了/不用了/放弃/先不管/驳回」→ 取消；否则 `rules.add_confirmed_rule()` 把老板答案落成**已确认规则**（席小核+席小文 prompt 都读 render_rules，永久生效）→ 拼 fix_task 派席小文按答案修改 → 挂 `wait_fix` 复用复审闭环）；`sweep_pipelines` wait_boss 超时不丢弃、重@老板提醒并重置计时。rules.py：新增 `add_confirmed_rule`（直接写「已确认」状态，老板答案本身就是确认动作）。
③**验证**：test_controller +4（doubt→wait_boss+@老板、老板答复→已确认规则+席小文派单+wait_fix、放弃词取消、非 admin 消息不拦截）、test_rules +1（add_confirmed_rule 直接已确认且进 render_rules）；tearDown 补 `_pipelines.clear()` 防测试间状态污染；全量测试绿（controller 71 + rules 6 + 其余各套）。
