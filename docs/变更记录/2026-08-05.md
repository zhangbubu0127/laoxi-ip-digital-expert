# 变更记录 2026-08-05

## 17:55 · Part A 排期表修复与增强 + 群内改表结构（controller/scheduler/base_bridge/intent/lark_bridge/identity + 测试 + 文档）

**起因**：老板 16:33「把详细脚本内容也放到里面吧，单开一列」被误执行成新增 3 行垃圾「曝光选题1/2/3」。根因：① 无「加列」意图 → LLM 误归「改排期」；② `_schedule_entries({})` 空参数静默编占位行。老板同时提了定稿后问排期 + 发布前 1 小时提醒 + 负责人列 + 权限分级 + 知识库语料分离（Part B 方向）。

**Part A 改动**（7 项，已全部落地）：
- **A1 禁止静默编造**：controller `_route_intent` 改排期 else 分支——params 空且内容无明确排期信息 → 回「你要排哪条选题、排哪天几点？」，不调 `_schedule_entries`。
- **A2 schema 8→10 列**：scheduler `_FIELDS`/`_HEADER` 加 `script`（脚本内容）+ `source_chat`（来源群，本地不同步）；`load_schedule` 兼容旧 8 格行补齐空串；`add_entry` 压平脚本单行；base_bridge `_FIELD_MAP` 加 `("script","脚本内容")`。
- **A3 群内加列**：intent INTENTS 加「改表格结构」+ `_TABLE_HINTS` 关键词（判定放改排期/写脚本前）；controller `_add_table_column`（列名解析 + 白名单：脚本内容→`base_bridge.add_column` + 自动填；负责人→已在；白名单外→创建但提示手动维护）；base_bridge 新增 `add_column`（`lark-cli +field-create`）。
- **A4 定稿→问排期→记录**：写脚本审核通过（wait_he pass）→ `_pipelines` 置 `wait_schedule` + 小席问「要不要上排期表？排哪天几点？负责人是哪个发片同事？」；老板/产品回「排 8/10 12:00」→ `_resolve_schedule` 组装 entry（脚本内容+负责人=发片同事+来源群=chat_id）→ add_entry；回「先不排」→ pop 不记；超时 sweep 安静撤。
- **A5 发布前 1 小时提醒**：scheduler `check_upcoming_publish`（今天 + 距发布 ≤60min + 待产/待发 + 有 source_chat，`_REMINDED` 内存去重）；lark_bridge `_maybe_send_publish_reminders`（30s 节流，owner=发片同事 → `publisher_open_ids[0]` 真 @，未登记回落文本点名）。
- **A6 数据清理**：本地 `workspace/排期表.md` 删「曝光选题1/2/3」3 行；多维表格 `+record-list` 核对后删 3 条记录（recvrprkewLLIA/recvrprlsT6SGC/recvrprn1rknJE），删前 `--dry-run` 确认。
- **A7 权限模型**：老板=产品=全权（含加列）；新增「用户」只读（看排期表/看完整讨论/查记录/反馈修改/追问），写脚本/改排期/加列一律拦「无权限」；identity `resolve_role` 加 `user_open_ids`；roles.json 加 `user_open_ids: []`。

**测试**：test_scheduler 8→14（+6：script 写 10 格/旧 8 格行加载/render 含新列/T-1h 命中/T-2h 不命中/去重）；test_intent 32→35（+3：改表格结构分类、不被写脚本误伤、LLM 路径）；test_controller 107→119（+12：A1 空参数问细节/有信息照排、A3 加列/用户拦截/列名解析、A4 审核通过置 wait_schedule/老板排期入列带 script+owner+source_chat/先不排不加行、A7 用户拦写脚本与改排期/只读放行排期表与反馈）。全量回归 260 例绿。

**文档**：架构书 v1.2→v1.3（§6 风格分库、§7.1 加列+问排期路由、§9 schema+只读镜像豁免+提醒+禁编造、§11 权限表、§13 错误处理；改前归档 1755）；PRD（AI 速读卡/模块 A 状态流/模块 G 排期表/第九章/第十一章 b+d，改前归档 1755）。

**重启**：只重启小席进程（PID 74745→84816，Part A 代码全在小席进程：controller/scheduler/base_bridge/lark_bridge）。启动日志干净。

**待老板提供**：发片同事 open_id（让发布提醒真 @，未登记时文本点名）；后续入群用户告知谁老板级/谁用户级，登记 `config/roles.json` 的 `user_open_ids`。

## 18:14 · Part B 知识库语料分离（架构变更：knowledge.py/rules.py/dispatch.py/xiaowen.py/controller.py + 测试 + 文档）

**起因**：老板要求「说话逻辑、说话风格分开几个库来产出脚本，使用的时候让小席问一下」。Part A 已定方向（架构书 v1.3 记「Part B 方向」），本次落地。

**Part B 改动**（4 项）：
- **B1 风格库目录 + 加载器**：新建 `knowledge/风格/老席/创作要求.md`（复制自 人设/老席创作要求会纪要.md）；knowledge.py 加 `load_style(user)`（缺省回落老席→老人设档案，绝不空）+ `style_users()`（扫 风格/ 子目录，老席兜底）。知识内容（业务事实/家长画像/爆款规律/合规红线/案例库/家长常问/竞对分析）保持通用共享，不进风格库。
- **B2 席小文按风格写作**：`XiaowenExpert(generate, style_user="老席")`；`_system` 改读 `load_style(self.style_user)` + `render_rules(style_user=...)`；风格指令以派单任务 `【风格:用户名】` 标记随行（主控→专家bot 唯一通道是任务文本，单/多 bot 两种形态同一套逻辑），`dispatch.run_expert` 用 `split_style` 剥离。
- **B3 小席问风格**：controller `_last_style[chat]`（复用）+ `_requested_style`（「按XX的风格写」直接点名免反问）+ `_write_script` 顶部守卫——`style_users()>1` 且未指定/未记录 → `_pipelines` 置 `wait_style` + 问「按谁的风格写」，答人名 → `_resolve_style` 登记并按原请求重新派单；`_dispatch_expert` 对席小文/席小习在非老席时给任务加 `【风格:用户名】` 前缀。
- **B4 学习规则按用户分库**：rules.py 全部函数加 `style_user`（老席沿用 `memory/学习规则.md`，其余 `memory/学习规则-<用户>.md`）；`learn_rules_flow`、`_confirm_rules`、`_resolve_doubt` 按聊天最近风格路由到对应库，席小文只读对应风格库已确认规则，不污染他人。
- 设计取舍：计划原写「学习规则.md 放 knowledge/风格/<用户>/」，但知识库事实层只读是硬约束 → **学习规则改落 memory/（学习层），风格库只放静态创作要求**，方向一致、分层不破。

**测试**：test_knowledge 5→9（+4：style_users 含老席/load_style 返回内容/未知用户回落/空回落）；test_rules 6→7（+1：张艺宝库与老席库隔离）；test_experts 28→29（+1：xiaowen 风格用户透传 load_style + 分库规则标签）；test_controller 119→125（+6：多风格库先问/单风格库不反问/答人名按风格派单/「按XX的风格写」直接点名/非老席派单带【风格:】标记/老席不带）。全量回归 272 例绿（含 knowledge 9）。

**文档**：架构书 v1.3→v1.4（§2 席小文/席小习、§6 风格库目录 + memory 学习规则分库、§7.1 加 wait_style 路由行、§10 学习机制按风格分库；改前归档 1813）；PRD（AI 速读卡 ⑤ 改已实现、模块 C 席小文按风格写作、模块 E 席小习分库、第九章 P1 风格分库→已实现，改前归档 1813）；CLAUDE.md（knowledge/ 加 风格/ 行 + memory 学习规则分库说明）；本篇 Part B 迭代记录。

**重启**：重启 席小文+席小习+小席 三进程（PID 69395/69436/84816 → 87401/87402/87403）。启动日志干净。

**待老板**：若要多风格库（如产品张艺宝也出脚本）→ 建 `knowledge/风格/<用户>/创作要求.md` 即可，群内写脚本会自动先问用谁的；目前仅老席，默认直用不反问。

## 16:34 · src/brain/controller.py + test_controller.py · 修复「说两条脚本只出一条」：意图提取的条数没传给席小文

**现象**：老板 16:27 在群里说「选题2给我出两个脚本」，席小文只出了 1 条；老板追问「为什么只出了一条」后，追问被派给席小文的同时审核管线也命中席小文，来回导腾很乱。

**根因**：意图层正确提取出 `count=2`（`params={'count':'2','topic':'选题2：…'}`），但 `_write_script(msg, content, task, rounds, replies)` 签名里没有 params 形参，`_route_intent` 派单时把 `result.params` 丢掉了 → 席小文的 task 里没有「2条」，它按 1 条写。条数在意图层与派单层之间断链。

**修复**：`controller.py` 三处——
- `_write_script` 加 `params: dict | None = None`，在 `_resolve_script_task` 后调 `_apply_script_count(task, content, params)` 把条数写进派单任务；
- 新增 `_apply_script_count`：`params["count"]` 存在且 >1、且 task 里没带该条数时，在任务尾追加「本批共 N 条脚本，围绕该选题写出 N 条不同角度/版本的脚本，逐条完整输出，每条以【席小文】## 脚本 标题 开头」；count 为 1 或已含条数则不重复；
- `_route_intent` 写脚本分支改传 `result.params`。
- 测试：test_controller 102→107（+5：count 追加到 task / 单条不追加 / 无 params 不追加 / `_write_script` 把 count 传给席小文 / 意图 params 的 count 一路传到派单）。
- 回归：controller 107 / intent 32 / experts 28 / context_store 10 / scheduler 8 / session 6 / circuit 5 / rules 6 / reflow 3 / material 6 / multi_main 12 全绿。
- 只重启小席进程（PID 69353→74745，唯一跑 controller.py 的进程），其余 5 bot 不涉及；启动日志干净。
- 待飞书群真跑验证：再让「选题N出两个脚本」→ 席小文应出 N 条。

## 16:26 · src/skin/multi_main.py + test_multi_main.py · 修复「选题依据要不到」：席小题依据存档解析不到 → 显示「未给出依据」

**现象**：老板 16:21 在群里问「给我依据」，席小题回复「选题N…\n依据：（未给出依据）」——选题配对上了、依据全空。

**根因**：依据存档内容完整（`workspace/上下文/…_席小题_basis.json` 3 条依据都在），但 LLM 产出的依据是**块状格式**「（选题1依据，来源：市场热点+知识库）\n依据段落」，而 `multi_main.py` 的 `_numbered_lines` 只认「数字.依据」行首数字格式 → 解析出空 dict → 配对时每条都落「（未给出依据）」。提示词只要求「第 N 条理由=第 N 条选题」没锁死行格式，LLM 自然飘成块状（更符合人读）。

**修复**：`multi_main.py` 新增 `_basis_map()`——优先解析「（选题N依据，…）\n段落」块状（`re.split` 抓序号 + 其后正文），解析不到再回落旧「N.依据」单行；`_paired_topics` 依据侧改用 `_basis_map`。两种格式都兼容，数据没丢只是解析器认不出。
- 测试：test_multi_main 10→12（+2：块状格式全量配对 / 块状格式指定第N条）。真实存档数据验证：3 条选题全部正确配对出依据。
- 回归：multi_main 12 / controller 102 / intent 32 / experts 28 / context_store 10 全绿。
- 只重启席小题进程（PID 69374→73342，唯一执行 `_match_basis_question` 配对逻辑的进程），其余 5 bot 不涉及；启动日志干净。

## 15:44 · docs/需求与决策记录.md + docs/iteration/ · 补本轮文档（归档+决策记录+迭代记录）

- `docs/需求与决策记录.md`（改前归档 `archive/需求与决策记录-20260805-1544.md`）：追加 §2.28「理解人话+说话即行动：承诺即兑现契约」——把 2.26「闲聊不承诺」/2.27「提议必须带标记」的契约升级线补全，避免决策记录停留在旧契约与代码不符。
- 新建 `docs/iteration/2026-08-05-理解人话与说话即行动.md`：本轮迭代记录（目标/三层改动清单/架构影响/验证/遗留），与 14:11 的「说到做到与提议闭环」（2.27 旧设计）区分开。
- 新建 `docs/讨论记录/2026-08-05.md`：补「为什么改」——老板反馈「智能体不够智能/说话不算话」的根因三层 + 整改对照（与变更记录时间流水互补；此前 8/4 缺档，8/3 为最近一篇）。
- 全部为文档追加/新建，无代码改动，无需跑测试。

## 15:37 · src/brain/controller.py + intent.py + 五个专家提示词 + test_intent.py + test_controller.py · 「理解人话+说话即行动」提示词调优（用提示词优化专家做，老板拍板架构级放开）

老板反馈（8/5 下午）：智能体「不够智能」——例子：老板问「刚才的选题你还能看见么」，小席回得暖心但只是空话（没真核对、没派单）。根因三层：
- ①「能看见」不在 14:00 建的关键词表（只认「看到/还能看到/还在/记录/找不回来/原始/丢/回顾/哪去了/哪里」）→ 例子被归闲聊；
- ② 闲聊分支执行依赖 14:10 的死板【提议】标记，LLM 把提议说成自然口语（「要不要让席小题…」）就不带标记 → `_register_proposal` 登记不了 → 空话；
- ③ 意图层没有「查记录」意图，这类 meta 查询全塌进「其他」。

修复（提示词+最小代码）：
- **intent.py**：INTENTS 增「查记录」；LLM 意图提示词补「刚才/上次/之前+回顾性动词 → 查记录，不是闲聊不是出选题」；`by_keyword` 加 `_LOOKUP_HINTS` 兜底（能看见/还在/在哪/哪去了/丢了/回顾/还记得/找不回来…，判定放「选题→出选题」之前），用例防误伤「把刚才那个选题写条脚本」仍走写脚本。
- **controller.py**：`_route_intent` 加「查记录」分支直通 `_lookup_topic_record(force=True)`；`_TOPIC_LOOKUP_PAIRS` 拓宽（含「能看见」变体）；`_lookup_topic_record` 加 `force` 参数；修正「存档 10 分钟清理」误导文案 →「只留最近一版」（basis 存档 14:00 起已持久，sweep 不碰）。**「说话即行动」契约**：新增【行动】JSON 协议——小席聊天回复末尾可带 `【行动】{"action":..,"task":..,"when":"立即|待确认"}`；立即动作（核对选题记录/看排期表）由 `_run_immediate` 真执行并把回执补进回复，待确认动作（重新出选题/写脚本/审核脚本）登记 `_pending_action` 等老板点头真派单；`_chat_reply` 登记后从显示剥离行动行。14:00 的「闲聊绝不承诺」升级为「承诺即兑现」——不压制你喜欢的自然回复，而是让承诺变真。**专家提议**：专家被直接@闲聊自回时，建议的可执行下一步带【动作名】标记（例「要不要我【重新出选题】按预算方向出3个？」）→ `_register_expert_proposal` 识别登记，老板回「好」即执行；覆盖单进程直接@路径（_route_intent）与多 bot 自回路径（handle_bot_output）。
- **五个专家提示词**（xiaoti/xiaowen/xiaone/xiaoxi/fupan）：各补「理解老板的话」few-shot（老板口语→垂类任务的正确理解，不删任何已认可规则）+ 闲聊建议下一步用【动作名】标记；席小核/席小习/席小盘严格不动解析依赖的输出格式。
- 测试：test_intent 29→32、test_controller 92→102。全量回归绿：controller 102 / intent 32 / experts 28 / context_store 10 / scheduler 8 / session 6 / market 7 / rules 6 / search 4 / pipe。
- 待飞书群真跑验证（老板拍板方式）：用「刚才的选题你还能看见么」「换个角度重新出一版」实测。需重启小席进程 + 涉及提示词的专家进程。
- 15:51 已全量重启 6 bot（小席=69353 / 席小题=69374 / 席小文=69395 / 席小核=69416 / 席小习=69436 / 席小盘=69472），启动日志干净，全部加载新提示词代码。
- **16:20 老板飞书实测通过**：问「刚才的选题你还能看见么」→ 小席真核对存档回执真实选题清单；说「换个角度重新出一版」→ 真派单席小题。两个例子均真执行，不再空话。【行动】JSON 立即/待确认两条路 + 专家【动作名】提议闭环验证通过。

## 14:10 · src/brain/controller.py + test_controller.py + 文档 · 「提议→肯定→真执行」闭环：小席提议带标记，老板点头后真实执行

老板需求：小席聊天里提议的下一步（重出选题/写脚本/审核）是好的想法，希望老板给肯定答复后小席真的去执行，不要只停在话术。
- **机制**：`_chat_reply` 改走 `_register_proposal` + `_strip_proposal_marker`——小席闲聊 prompt（`_default_chat_generate`）规定：主动提议下一步必须句末加固定标记【提议】<动作>：<内容>；`_register_proposal` 用 `_PROPOSAL_RE` 解析并登记 `_pending_action[chat_id]`（intent/params/task/ts），对外展示剥离标记（老板看到的是自然话术，不知道内部标记）。
- **肯定触发**：`handle_message` 在 wait_boss 检查后、确认已发布前插入肯定语检查——老板/产品回短肯定词（`_is_affirmative`：好/要/可以/行/嗯/好呀…8字内）且 `rules.has_pending()` 为空 → `_pop_pending`（3600s 过期）命中 → `_route_intent` 按登记意图真派单（出选题→席小题 / 写脚本→席小文+席小核 / 审核脚本→席小核 / 看排期表→直发）。无待定动作时肯定词不劫持，走正常意图路由。
- **动作白名单**：`_PROPOSAL_INTENTS` 限 出选题/写脚本/审核脚本/看排期表；删掉 `("排期","改排期")` 映射——改排期需具体日期选题，聊天锁定不了，肯定回复会把细节丢成「好」→ 落占位垃圾（与 12:35 bug 同类）。2.26「闲聊不承诺契约」升级为「提议必须带标记，带标记=承诺、无标记=纯话术」。
- **复用**：派单/排期/素材/圆桌等原 inline 分发链抽成 `_route_intent(msg, result, rounds, replies)`（行 508-587 → 1 行），提议确认与正常意图共用同一执行链，防两套逻辑漂移。
- 测试：test_controller 84→92（+8：登记捕获/脚本意图/改排期拒绝/chat_reply 登记+剥离标记/肯定触发出选题真派单/肯定触发写脚本流水线/无待定安全/肯定看排期表）。全量回归绿：controller 92 / context_store 10 / intent 29 / experts 28 / scheduler 8 / session 6 / market 7 / rules 6 / search 4。
- 文档：需求与决策记录 §2.27（改前归档 archive/需求与决策记录-20260805-1410.md）。
- 重启小席进程（PID 53671→55387，controller 逻辑所在进程），其余 5 bot 不涉及；启动日志干净。

## 14:00 · src/brain/context_store.py + controller.py + test_*.py · 修复「小席说到做不到」：查选题记录只聊天不真核对 + 存档被 sweep 清掉

**现象**（日志 13:35 还原）：老板问「刚才的选题你还能看到么」→ 小席回复「我这就去核对原始记录确认一下，同时要不要让席小题按刚才那个方向先重新整理一版，给您双保险？」——但意图被 LLM 归「其他」，只走 `_chat_reply` 纯闲聊生成，**没有任何真实动作**（没核对记录、没派单席小题）。

**根因**（三层叠加）：
- ①**sweep 清存档**：`skin/lark_bridge.py` 每轮调 `context_store.sweep(600)`，`sweep()` 把目录里所有超 10 分钟的文件删掉，包括 `_basis.json`/`_review.json` 存档（`take()` 特意排除存档，`sweep()` 没有）。席小题今天 10:12/10:30/11:48 三次出题都存过 basis，全被清 → `workspace/上下文/` 全空。
- ②**会话记忆是进程内存**：`session.py` 7 轮 `OrderedDict`，进程重启即空；小席 12:28 重启过 → 历史选题记忆也没了。
- ③**「查选题记录」意图归「其他」**：controller else 分支只会闲聊生成，无任何查记录动作。

**修复**（数据根→行为梢三层）：
- `context_store.py` `sweep()`：跳过 `_basis.json`/`_review.json` 存档，只清临时派单上下文（与 `take()` 排除对齐）——选题清单/审核详情从此持久，老板随时能核对到。
- `controller.py` else 分支加 `_lookup_topic_record()`：识别「查/核对选题记录」类请求（触发词对：选题+看到/还能看到/还在/记录/找不回来/原始/丢/回顾/哪去了/哪里）→ 真读 basis 存档 → 有则回执真实选题清单（含类型标签）；无存档但有会话记忆席小题产出 → 回执记忆里的；无存档但记忆有席小文产出（方向可恢复）→ **真实派单席小题按方向重出一版**；都无 → 如实说「没找到」（存档 10 分钟清理/进程重启清记忆）+ 引导老板给方向或说「重新出选题」。
- `controller.py` 闲聊提示词强化：**绝不承诺「我这就去核对/查/派单/处理」等未执行系统动作**（闲聊不触发真实操作，说了等于谎报），需要查记录引导老板说明确指令。
- 测试：test_context_store 9→10（sweep 保留 basis/review），test_controller 79→84（查记录 from_basis / from_memory / none_honest / 真派单重出 / handle_message 意图其他走真核对）。全量回归绿：controller 84 / context_store 10 / intent 29 / experts 28 / scheduler 8 / session 6 / market 7 / rules 6 / search 4。
- 重启小席进程（PID 47204→53671，唯一跑 sweep 的进程），其余 5 bot 不涉及。

## 12:35 · src/brain/controller.py + intent.py + test_*.py + workspace/排期表.md · 修复「老板确定选题/类型不能及时同步排期表 + 多维表格」

**根因**（日志 12:00-12:02 还原）：`_try_pick_topic`（controller.py）只有 `_PICK_HINTS`（排上/排期/定了/选这个/第）能触发，且依赖 `_last_expert_output` 返回席小题产出；老板确认语「这条选题通过了，把类型和选题都上传」「那可以排期发布了」不触发 → 落兜底 `_schedule_entries` 生成占位「曝光选题1/2/3」+ 类型硬编码「曝光」→ `add_entry` 同步多维表格 → 垃圾入库。深层缺环：写脚本流水线（wait_wen→wait_he→wait_boss）定稿时没记录「刚定稿脚本对应的选题 + 类型」，老板说「这条」时系统无从追溯真实选题。

**修复**：
- `controller.py`：新增 `_draft_ctx`（chat_id → (topic, content_type)，`_write_script` 开头经 `_pipe_ctx` 从 content/task 解析「第X个」→ basis 定位选题行 + `_type_from_line` 正则 `【曝光|留资|信任】` 提取真实类型）+ `_last_passed`（chat_id → {topic, content_type}，两个定稿出口——wait_he verdict=pass 与 wait_boss `_is_pass` 放行——调 `_record_passed` 升格）。`_try_pick_topic` 增 chat_id 参数 + `_PASSED_HINTS`（通过了/排期发布/发布/上传/这条/这个）：① 命中 `_last_passed` → 返回真实选题+类型；② 回落「第X个」→ 类型从行首解析，不再硬编码「曝光」；③ 都没有 → 返回 None。改排期 handler 增「没找到刚定稿的选题」明确回执（防重启后再次生成占位）。
- `intent.py`：改排期 LLM 描述补确认语示例；`by_keyword` 在「出选题」前加规则——`("通过" in content and "选题" in content) or ("排期" in content and "发布" in content) or ("上传" in content and ("选题" in content or "类型" in content))` → 改排期，防被「选题」关键词吸到出选题。
- 测试：test_controller 76→79（test_pick_passed_topic_uses_finalized / test_pick_nth_topic_parses_type / test_pick_no_source_returns_none），test_intent 28→29（test_confirm_passed_topic_synonym）。全量回归绿：controller 79 / intent 29 / experts 28 / scheduler 8 / market 7 / rules 6 / search 4。
- 数据清理：`workspace/排期表.md` 删 9 行「曝光选题N」占位（保表头）；多维表格删 3 条占位记录（recvrokJjENQL4/recvrokKdgIxte/recvrokLppCy4W，选题列全是「曝光选题N」），删前 `+record-list` 确认无真实数据。
- 重启小席进程（PID 36096→47204，controller 逻辑所在进程），其余 5 bot 不涉及；启动日志干净。

## 11:40 · 文档同步：架构书/PRD/智能体团队简介/需求与决策记录 对齐本次市场情报能力 + 各出一半 + wait_boss 放行

- `docs/架构书.md`（v1.1→v1.2，改前归档）：§5 席小题职责补市场情报能力 + 各出一半配额；§6 知识库树补 竞对分析/ + radar/；§7.1 路由表补「更新市场情报」行；§12 数据流⑧ 与 §13 错误处理补 wait_boss 三选一（答案/放行/取消）。
- `docs/PRD.md`（改前归档）：AI 速读卡补市场情报库；§2.3 导航规则补「更新市场情报」行；模块 B 席小题补市场情报库三来源 + 各出一半配额 + 依据标来源。
- `docs/智能体团队简介.md`（改前归档）：小席路由表补「更新市场情报」行；§2 席小题补市场情报能力（竞对图谱/中新对比/雷达热点/竞对线索）+ 各出一半配额。
- `docs/需求与决策记录.md`（改前已归档 0938）：追加 §2.24 市场情报能力 + 各出一半 + wait_boss 放行（含实时搜索尽力而为的诚实前提）。

## 12:05 · 变更记录自查补漏（老板要求「更新时看过变更记录，别有遗漏」）

- 复核本次会话改动 vs 变更记录，发现 2 处遗漏，补记于此：
- ①`.gitignore` 补 `radar/feishu_auth_qr.png`（GitHub 安全自查时发现飞书扫码登录截图未忽略，加入，防裸 `git add .` 带出）。
- ②**6 bot 全量重启**（根因：10 个选题没用雷达内容——bot 进程 8/4 23:04 启动、市场情报代码 8/5 09:32 改，旧进程加载旧代码；重启后 PIDs 小席=36096、席小题=28902、席小文/席小核/席小习/席小盘=26939-26942，全部加载新代码，已确认选题能用上热点）。
- 已确认无其他遗漏：市场情报能力（09:44）、各出一半（10:25）、wait_boss 放行（11:30）、文档同步（11:40）均已登记；iteration/2026-08-05-市场情报能力.md 已建。

## 11:30 · src/brain/controller.py + test_controller.py + memory/学习规则.md · 修复「wait_boss 下老板放行话术被误当答案」死循环（老板反馈：说「确定了可以下一步了」系统听不懂，反复产出文章、进程无法继续）：

①**根因**（日志证据链 11:15-11:16）：写脚本流水线 wait_boss 挂起（席小核审核疑问暂停 @老板）时，`handle_message` 无条件拦截 admin 消息走 `_resolve_doubt`，而 `_resolve_doubt` 只有「取消」和「落规则+让席小文改+复审」两个出口——没有「放行/通过」出口。老板说「确定了可以下一步了，就这么样」（放行语义）被当实质答案：落成垃圾规则 + 派席小文再改 → 复审又出疑问 → 又 @老板 → 死循环。不是 LLM 听不懂，是主控硬拦截 + 无放行出口。
②**修复**：`_resolve_doubt` 加第三个出口——放行（`_PASS_HINTS` 明确短语 + `_PASS_EXACT` 精确短词：确定了/可以下一步/就这么样/就按这个来/可以了/继续/没问题…）：命中则按当前脚本通过定稿、pop 流水线，不落规则、不派改稿；`_is_pass` 放行词表避开「没问题/可以/按这个来」等会出现在实质答案里的词，防误伤「费用按8万算没问题」。两处 wait_boss 提示文案补三选一说明（给答案/可以了通过/不用了取消）。
③**数据清理**：删掉本次 bug 误落的垃圾规则 rule_0002「就按这个来吧」、rule_0003「确定了可以下一步了，就这么样」（会污染席小核/席小文 prompt），保留 rule_0001。
④**验证**：test_controller 76 全绿（+3：放行定稿不落规则、短放行词 4 变体、实质答案「费用按8万算没问题」仍落规则回归）。重启小席进程。

## 10:25 · src/brain/experts/xiaoti.py + test_experts.py · 席小题选题「各出一半」取材配额（老板要求：不要全热点/全竞对/全本地库）：

根因：此前席小题 _system() 把所有来源（爆款规律/家长原话/问题清单/素材库/竞对情报）一次性拼进 prompt 让 LLM 自由选，易被单一来源带跑。修复：handle() user prompt 加【取材配额】——选题必须一半取材【竞对情报/市场热点】段（含市场热点/竞对/中新对比，竞对不得独占这一半）、另一半取材本地知识库（爆款规律/家长原话/家长典型问题清单/素材库），禁止全部来自单一来源；每条【依据】开头标注取材来源（来源：市场热点/来源：竞对/来源：知识库）供老板验证配额；数量从 task 解析（`_extract_count`，约 N 条，无数字则模糊配额）。test_experts +2（含数量解析、无数量回落）。只重启席小题进程（PID 28902），其他角色不涉及。

## 09:44 · 市场情报能力落地（竞对情报库 + 实时搜索 + 热点雷达接入）+ 雷达项目迁入本仓库

老板需求：席小题要能拿「互联网上新加坡留学热门选题、竞对有哪些、竞对与我们相比的劣势」当选题原料。此前核实：席小题 `_system()` 只读本地 6 类知识文件，全系统唯一外搜是席小核维基百科事实核对，knowledge/ 无任何竞对数据，热点雷达是独立项目未接入。老板拍板「三个能力都要有」+「雷达接进来」+「雷达直接搬进本仓库，GitHub 上传完整自包含」。

①**架构**——统一「市场情报库」`knowledge/竞对分析/`，写入方两个、读取方一个：
- 写入方 ① 雷达 run.py 后跑 `scripts/sync_radar.py`（机器写热点）；② 主控「更新市场情报」意图 → `brain.market.refresh()`（实时搜索尽力而为）。
- 知识层只读：`竞对图谱.md`（人工/Claude 润色，WebSearch 真实竞对）+ `中新教育对比.md`（人工润色，提炼自雷达 edu_compare）+ `热点话题.md`（机器覆盖更新）+ `竞对线索.md`（refresh 原样存线索，供人工挑着入库）。
- 读取方：席小题 `_system()` 每次出选题重拼 prompt 注入【竞对情报】段，读最新情报无需重启。

②**实时搜索现实约束（诚实前提）**——无 key 免费搜索端点全被风控：Bing RSS curl 出真结果、requests 常返回无关；DuckDuckGo 202 挑战页；百度需登录。结论：实时搜索只做「尽力而为」补充层，可靠主路径=雷达每日同步（热点）+ Claude 会话 WebSearch 润色入库（竞对图谱）。`market.py` 用 requests+Bing RSS+`_SG_WORDS` 相关性过滤（新加坡/狮城/留学/NUS/低龄/陪读…），任何失败返回空，永不 raise、永不阻塞出题。要绝对可靠运行时搜索需付费 API key（新依赖+密钥=红线），留待老板决策。

③**新增文件**：`src/brain/market.py`（search_web/hot_topics/competitor_leads/intel/refresh，复用 search.py try/except→[] 模式）+ `test_market.py`（7 用例全绿）+ `scripts/sync_radar.py`（读雷达 output 最新 `radar_YYYY-MM-DD.csv` → 生成热点话题.md；同目录 edu_compare.json 且目标不存在时生成中新教育对比.md 种子；--dry-run/--radar-output 参数）+ `knowledge/竞对分析/`（README.md + 竞对图谱.md + 中新教育对比.md + 热点话题.md）。

④**修改**：`experts/xiaoti.py`（顶部 import market_intel，默认立场补「竞对/市场劣势当反常识钩子」，_system() 素材库行后插【竞对情报】段）；`intent.py`（INTENTS 增「更新市场情报」+ LLM 提示词描述 + `_MARKET_HINTS` 关键词兜底：更新情报/更新市场/搜竞对/看竞对/搜热点/现在什么话题热/刷新市场情报 等）；`controller.py`（import market、`_ADMIN_ONLY` 增「更新市场情报」、新增 `_refresh_market()`——成功回执「市场情报已更新：热点 N 条、竞对线索 M 条」+ 失败回执「实时搜索被风控，情报库保持上次数据」提示雷达同步）；`test_intent.py` +1、`test_controller.py` +2、`test_experts.py` +1。

⑤**雷达迁入**——`radar/` 整目录 `mv` 进老席仓库（run.py 用 `ROOT=dirname(abspath(__file__))` 位置无关，直接可用 system python3 无需 venv）；`.gitignore` 补 `radar/config.json`（base_token）、`radar/scrapers/.douyin_profile/`（抖音登录态）、`radar/output/`、`radar/.venv/`，裸 `git add .` 不会带出密钥；`radar/CLAUDE.md` 目录约定改为仓库内路径 + 每日流程补 step 4「同步席小题情报库：`python3 <老席根>/scripts/sync_radar.py`」。

⑥**验证**：全量测试绿（test_market 7 + test_experts 26 + test_intent 28 + test_controller 73 + test_search + test_rules）；`scripts/sync_radar.py --dry-run` + 真实跑生成热点话题.md 内容正确；`from brain.market import intel; print(intel()[:300])` 含竞对图谱/热点无异常。
