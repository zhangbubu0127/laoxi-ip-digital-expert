# 变更记录 2026-08-08

## 09:36 · planner.py + controller.py + test_controller.py + 排期表 + 架构书/PRD/iteration · 确认已发布/数据回流自然语言化

**老板反馈**（飞书 09:24-09:26）：「昨天发布的视频今日浏览3450、点赞87…为我录入；并且复盘」→ 小席回「当前没有待回流数据」；「《新加坡私校集体收紧，初中直通本科这条路要断了吗？》已发布，更新，并且记录刚刚的数据」→ 小席回「格式不对，示例：8/3 普娃逆袭 已发布」。老板质疑「我自然语言说的你不能给我转换成标准格式么」。

**根因**（代码核实）：
- `_try_mark_published` 只认 `M/D 日期 + 选题` 字面格式，`《标题》已发布`（无日期、带书名号）→ 正则不命中 → 回「格式不对」。
- `_parse_reflow_data` 要求 `M/D 日期` 开头，自然语言报数据（无日期）→ 返回 None → 落到「没有待回流数据」分支，即使排期表已有「已发」条目。
- planner 的 `params`（结构化 date/topic/data）被两个执行器整体丢弃，只重新正则解析原始 content。

**改动**：
- `controller.py`：
  - `_try_mark_published(content, params)`：优先用 params.date/topic；正则兜底增强——剥《》「」标点后按标题**模糊匹配**待发行（无日期也认），`_normalize_topic`/`_fuzzy_topic_match` 归一化比较；匹配失败回「没找到匹配的待发条目」。
  - `_reflow(msg, content, replies, params)`：优先用 params.date/topic/data；无日期但带数据指标 → `_extract_metrics` 正则提取（浏览/播放/点赞/评论/收藏/转发/留资 + 数字）→ 按**最近一条「已发」行**回落存档；`_latest_published_row` 按日期取最近。
  - `_exec_mark_published`：**连动**——标完「已发」若同消息/参数带数据 → 直接对该行回流。
  - `_ACTION_EXECUTORS` 数据回流 lambda 透传 params。
- `planner.py`：判断段补「报数据→数据回流，params 放 date/topic/data」一行；params 契约扩 `data`，要求确认已发布/数据回流把能认出的 date/topic/data 填进 params。
- `workspace/排期表.md`：8/7 19:17《新加坡私校集体收紧…》补标 `已发`/`待回流`（运维，老板已在群里确认已发布）。

**测试**：test_controller 新增 5 条——自然语言报数据无日期回落已发行 / reflow 吃 planner params / 《标题》已发布无日期模糊匹配 / 标已发吃 params / 已发布+数据连动。controller 159 + 全量回归（intent 44 / experts 31 / scheduler 17 / planner 14 / rules 11 / context_store 10 / knowledge 9 / market 7 / session 6 / material 6 / pipe 5 / identity 5 / bots 5 / circuit 5 / used_topics 5 / reflow 3 / search 4）全绿。

**文档**：架构书 v1.5→v1.6（§4.1 params 契约 + §7.1 路由表 3 行 + §10.2 自然语言录入 + §12 ⑨⑩）；PRD（AI 速读卡 2026-08-08 新增 + 模块 A 职责 + 模块 F 自然语言录入 + 模块 G 已发确认）；新增 `docs/iteration/2026-08-08-数据回流与已发布自然语言.md`。改前已归档 `架构书.md-20260808-0936.md`、`PRD.md-20260808-0936.md`。

**遗留**：老板「记录刚刚的数据」若指前一条消息（无数字）仍无法连动——数据与已发布确认分两条消息时，报数据那条需已发行存在才入账；跨消息暂存数据未做（需要时另开一轮）。

## 09:50 · controller.py + scheduler.py + test_controller.py + 架构书/PRD/iteration · 跨消息暂存待回流数据（解决上轮遗留）+ 修 待产/待发 bug

**改动**：
- `controller.py`：模块态 `_pending_reflow`（7200s TTL）+ `_last_marked`；`_reflow` 报数据无「已发」行或行未标已发 → `_stash_pending` 暂存回「数据已记下…」；`_exec_mark_published` 标完已发后 `_take_pending` 命中自动补回流（带日期/标题的暂存只在匹配刚标已发行的行时应用，防串台）。
- `scheduler.py` `mark_published` + `controller.py` `_try_mark_published` 模糊循环：匹配范围 `待发`→`待产/待发`——真实排期行创建即「待产」（4 处 add_entry），此前只认「待发」导致真实行无法标已发（潜在 bug）。
- 测试新增 3 例（先报数据后确认已发布自动回流 / 带选题暂存确认另一条不误配 / 超 TTL 不补回流）；controller 162 + scheduler 17 + 全量回归全绿。
- 文档：架构书 v1.6→v1.7（§7.1 路由 +1 行、§10.2 跨消息暂存、规则4 待产/待发）；PRD（AI 速读卡 + 模块 A/F/G）；iteration 记录补第二轮、遗留①解决。改前已归档 `架构书.md-20260808-0950.md`、`PRD.md-20260808-0950.md`。

## 10:15 · controller.py + test_controller.py · 修 params 路径精确匹配失败即放弃（生产复现 10:09「未找到对应待发条目」）

**现象**（测试群 10:08-10:09）：张艺宝「这个我昨天在另一个群里已确认发布，你这有记录么，“8/7 19:45《孩子偏科…》待发布，核对是否准备好。”」→ 小席回「已发状态：未找到对应待发条目」。

**根因**（日志核实）：planner LLM 把「8/7 19:45」整个塞进 params.date，排期行 date 只有「8/7」；`_try_mark_published` params 路径精确匹配 `mark_published("8/7 19:45", topic)` 失败 → 直接回「未找到对应待发条目」，**不降级到模糊匹配**（v1.6 引入的缺陷）。另：用户称「昨天在另一个群已确认发布」，但老席留学ip数字专家群历史无任何「已发布」确认，孩子偏科行本就不该是已发。

**改动**：新增 `_resolve_publish_row(date_hint, topic_hint)`——date 剥出 M/D（去掉时间）、topic 剥《》标点后模糊匹配待产/待发行；`_try_mark_published` 的 params 路径与正则路径都先经解析器再标，不再精确匹配失败就放弃。日期剥出且对不上时不强配（防误标错行）。

**测试**：新增 `test_mark_published_params_time_in_date_falls_back`（复现生产：params.date="8/7 19:45" 落到 8/7 待产行并标已发）；controller 163 + scheduler 17 全绿。小席已重启（10:14）。

**运维（10:16，老板确认后）**：孩子偏科行标「已发」（老板口头确认已发布，作为人工确认）；新加坡私校行补回流 09:24 报的「浏览3450 点赞87 评论3 收藏2 转发3」（之前老代码没入账）→ 状态回流完成；派 @席小盘 复盘（10:17 出分析：政策风险类抓眼球但留资弱，建议下回改「哪些孩子还能直通本科」+「可领方案」钩子），已写 memory/数据回流.md + 验证结论.md。

## 11:01 · 席小题不出选题排障（生产复现 10:50-10:51）+ 重启 + 测试群验证

**现象**：孙芳洁「帮我出五个中本贯通的选题」→ 群里无回复。**根因核实（非代码改错）**：①系统日志 10:51:22 主控 LLM 崩后降级关键词路由，`动作命中 action=出选题`→`派单 席小题`，路由正常；②卡点在 DeepSeek API 间歇性 ECONNREFUSED（外部，主控 10:50/10:51 同崩）+ 席小题进程已跑 17h（昨 18:07 启动，log 36 次 ECONNREFUSED，10:57:22 API 已恢复仍连不上，同时刻主控 4 次调用全成功）+ `multi_main.py` 专家异常吞掉只记日志不回群（违反「异常绝不静默」）。

**改动**：杀席小题旧进程树（58175/58183/58189），同命令重启（新 PID 4294，11:00:20 启动）。测试群（oc_eed170...）派单验证通过：席小题产出 5 个中本贯通选题 + 「需要看依据么」，链路全通。

**遗留**：`multi_main.py` 静默吞异常未改（待老板定，拟加「明说失败」回复）；5 个选题待老板定是否转发生产群给孙芳洁。

## 11:46 · 新增市场调研能力（席小题 research）+ 搜索源切 Tavily

**需求**（老板）：席小题接调研——老板在主控说「调研一下X」→ 小席派单席小题 → 实时搜索 → 席小题在群里带来源回答。一步成文、回答带来源。

**改动**：
- `brain/research.py`（新建）：调研流程。`research(question, top=5, generate)` → `_queries` 剥前缀（长词优先「帮我调研一下|调研一下|帮我查查…」）拆连词 → `market.search_web(q, sg_filter=False)` → DeepSeek `_RESEARCH_SYSTEM` prompt 带来源成文（结论先行≤400字、区分「搜到的/无法确认的」）；无结果诚实回、LLM 挂掉回一手搜索结果、外层 try/except 永不 raise。
- `brain/market.py`：`search_web` 加 `sg_filter=False` 参数（非新加坡限定主题时跳过新加坡关键词过滤）。**搜索源切 Tavily**：`search_web` 优先 `_tavily_search`（读 `config/secrets.json` 的 `tavily_api_key`），无 key 或失败回落 `_bing_rss`。Tavily POST `api.tavily.com/search`，Bearer 认证，映射 title/url/content。
- `brain/experts/xiaoti.py`：加 `research(question)` 方法，复用 DeepSeek generate，绕过选题 prompt。
- `brain/experts/dispatch.py`：`RESEARCH_MARK="【调研】"`，席小题 task 以 `【调研】` 开头走 `research()`。
- `brain/planner.py`：ACTIONS 加「调研」+ 判断段（老板要调研/查证市场信息→动作=调研，派席小题实时搜索）。
- `brain/controller.py`：`_exec_research`（从 task/params 取问题，`【调研】` 前缀派席小题，不设 pipeline）+ `_ACTION_EXECUTORS["调研"]` + `_INTENT_TO_ACTION["调研"]`；权限走 admin-only。
- `brain/intent.py`：`_RESEARCH_HINTS`（调研/查查市场/了解下最新/什么行情等），放在「出选题」判断前防「调研下这个选题」被误吞。
- `src/skin/multi_main.py`：席小题「【依据】」主题拆分加 `not task.startswith("【调研】")` 守卫，调研输出不被拆选题。
- `config/secrets.json`：加 `tavily_api_key`（gitignored，不进 commit）。

**搜索源决策**：Bing RSS 在中国网络路径被破坏（重定向 cn.bing.com 出垃圾结果）、DDG 被屏蔽 → 实测后改走 Tavily（SaaS API，免费层~1000次/月）。实测 key 有效（HTTP 200），「中本贯通」搜索返回真实相关结果。

**测试**：`test_research.py` 新建 8 例；test_intent +5（调研同义词、「调研下这个选题」不被出选题吞）；test_planner +2；test_controller +2（调研派单带标记/单bot兜底调 research）；全量回归全绿（controller 165 / intent 45 / planner 15 / research 8 / experts 31）。

**运维**：小席（13885）+ 席小题（13886）整树重启加载新代码（11:45）。集成验证：`research.research('调研一下中本贯通…')` 真实搜索+成文通过；`run_expert('席小题','【调研】…')` 路由通过。**待真群验证**：测试群（oc_eed170...）发「@小席 调研一下X」走完整链路。

## 11:58 · 修调研生成被截断（生产群 11:52 复现）+ llm 截断统一重试

**现象**（生产群 11:51「@小席 调研下中本贯通和国际高中有什么区别」）：测试群 30 秒前同句正常，生产群席小题却回「调研回答生成暂时连不上模型」+ 原始搜索——老板误以为断网。

**根因**（系统日志 11:50/11:51 对比）：`deepseek-v4-flash` 是推理模型，research 生成 `max_tokens=1000` 太紧，thinking 吃满预算 → `finish=length`。测试群那次是「1000 token 全被推理吃掉、正文为空」→ llm.py 走「空输出加大预算重试」→ 成功；生产群那次是「推理 595 + 回答 405 被截断、有部分内容」→ llm.py `if content and finish==length` 分支**直接 raise** 不重试 → research.py 捕获后回误导性「连不上模型」。同代码、同句，纯截断时机差异，非两群差异。

**改动**：
- `llm.py`：`finish=length` 无论正文为空还是有部分内容，一律并入「带直接输出提示、加大预算（max_tokens+4000/次，上限16000）重试最多 2 次」；不再对有内容截断直接 raise。全局受益（planner/写脚本同样会撞截断）。已确认无调用方依赖原 raise 行为。
- `research.py`：`max_tokens` 1000 → **3000**（给推理留余量，减少触发截断）；兜底文案「调研回答生成暂时连不上模型」→「调研回答生成没成」（截断≠断网，不再误导）。

**测试**：test_research 兜底文案断言同步更新；controller 165 / intent 45 / planner 15 / research 8 / experts 31 / rules 11 / scheduler 17 / search 4 / session 6 / material 6 / pipe 5 / identity 5 / bots 5 / circuit 5 / used_topics 5 / reflow 3 / knowledge 9 全绿。真实调研调用（陪读政策）带来源输出正常。

**运维**：小席（16375）+ 席小题（16376）整树重启加载 llm.py/research.py 改动（11:57）。**待验证**：生产群重发「@小席 调研…」确认不再回「连不上模型」。

## 16:22 · 业务口径固化：国际中本贯通 vs 新加坡留学 不互比（老板 2026-08-08 群确认「这俩项目现在同时推」）

**需求**（老板）：修改业务核心点——「不要用国际中本贯通和新加坡留学对比，这俩项目现在同时推」。扫现有知识库，往后输出永不比较两者优劣势，各说各的；打竞品只打其他留学项目 + 普通国内中本贯通。

**改动**：
- `knowledge/业务事实/竞争口径.md`（新建）：定位规则唯一事实源，含 4 条核心口径（不互比/禁这类表述/打竞品范围/国际中本贯通不是竞品）。
- `knowledge/业务事实/项目详情.md`：品牌定位下加「并列项目（2026-08-08 确认）」说明；竞争对比表后加「不打国际中本贯通」note。
- `knowledge/README.md`：分类映射加「业务事实 | 竞争口径.md」一行。
- `src/brain/experts/xiaoti.py` `_system()`：加载 `业务事实/竞争口径.md`，prompt 加【项目竞争口径（必守）】段。
- `src/brain/experts/xiaowen.py` `_system()`：同上接入。
- `src/brain/experts/xiaone.py` `handle()`：红线/费用旁加载竞争口径，prompt 加【项目竞争口径（必守）】段（审核据此拦「对比」违规）。
- `src/brain/research.py` `_RESEARCH_SYSTEM`：加一条——调研涉及国际中本贯通/新加坡留学时各说各的，不得比较谁好谁坏、不得暗示二选一。

**测试**：test_experts +3（xiaoti/xiaowen/xiaone system 均含「项目竞争口径」「不得互相比较优劣势」）、test_research +1（调研 system 含「各说各的」「不得比较谁好谁坏」）；全量回归全绿（experts 34 / research 9 / controller 165 / intent 45 / planner 15 / scheduler 17 / rules 11 / reflow 3 / session 6 / material 6 / search 4）。

**生效机制**：knowledge `.md` 为运行时读取（下次出题/写稿/审核/调研自动生效，无需重启）；`.py` prompt 改动需重启 小席/席小题/席小文/席小核 加载。**待重启**。

## 17:30 · controller.py + test_controller.py + 排期表 + 飞书多维表格 · 修排期错乱（占位垃圾行）+ 查选题答非所问

**老板反馈**（生产群 oc_03cf…，17:10-17:12）：「排8/8 18:15 负责人张艺宝」→ 系统生成 3 条垃圾占位行（8/8 曝光选题1/2/3，负责人=席小文，类型=曝光），应为 1 条正确行（选题3=「19岁本科毕业，在新加坡能找到什么工作？真实工资单给你看」，类型=留资，负责人=张艺宝）；「我选择出脚本的选题你还能看到么」→ 答非所问，dump 整版 5 个选题，不回刚定稿/选中的那一条。

**根因**（代码核实 + 生产日志 17:07-17:12 比对）：
- Bug 2：17:07:04 脚本定稿 → 流水线进 `wait_schedule`，但 17:10:11 用户来排期已超 `_PIPE_TIMEOUT=180s`，`sweep_pipelines` 先清掉 wait_schedule → 消息落到 planner→改排期→`_exec_change_schedule`→`_try_pick_topic` 返回 None（「排8/8…」既不中 `_PASSED_HINTS` 也不中 `_PICK_HINTS`）→ `_schedule_entries` **count 默认 3、owner 写死席小文、type 默认曝光、topic 编造「曝光选题N」** 生成 3 条垃圾行。`_last_passed`（刚定稿选题）是模块态、流水线被 sweep 后仍在——修复落点。
- Bug 1：planner 判「…还能看到么」为查记录 → `_lookup_topic_record` `force=True` 直接读 `load_basis` 整版 dump，不看 `_last_passed`。

**改动**（`controller.py`）：
- 新增 `_parse_owner`/`_list_reference`/`_explicit_count`/`_has_schedule_intent`/`_parse_count`/`_asks_chosen` 六个小工具。
- `_try_pick_topic` ① 条件放宽：有 `_PASSED_HINTS` **或** 明确排期意图（日期/时间/负责人/排字/params.date）→ 用刚定稿选题，owner 走 `_parse_owner`；「第N个」「N条/个」显式排除（仍走②列表挑选/批量）。
- `_schedule_entries` 只服务批量排期：无明确数量（内容或 params.count）返回 `[]`，owner 不再写死席小文。
- `_exec_change_schedule`：`_schedule_entries` 空 → 明说「没指定排哪条选题或排几条」绝不编占位；成功回执补负责人。
- `_lookup_topic_record`：问的是刚选中/刚定稿那条（选+脚本/选择/出脚本/定稿/刚通过）→ 优先回 `_last_passed`/`_draft_ctx` 那一条，不回整版 dump。
- `_build_schedule_entry` 复用 `_parse_owner`。

**测试**：test_controller 新增 7 条（排期用 last_passed+date+owner / 批量 count 不误用 / 列表序号不误用 / entries 无数量空 / 内容带数量 3 条 / 无数量问细节不编占位 / lookup 优先 last_passed），controller 165→172，全量回归（scheduler 17 / intent 45 / planner 15 / experts 34 / research 9 / rules 11 / session 6 / material 6 / pipe 5 / identity 5 / bots 5 / circuit 5 / used_topics 5 / reflow 3 / search 4 / context_store 10）全绿。

**运维**：删排期表 3 条垃圾行 + 补正确行（8/8 18:15 留资 选题3 张艺宝，来源群=oc_03cf…）已同步飞书多维表格（upsert 正确行 + `record-delete` 删 3 条孤儿记录 recvrH8bAHrbWP/recvrH8dMenoqM/recvrH8fJWH41z，现 5 行干净）；小席整树重启（旧 47922 → 新 57890，17:29 起）。

**遗留**：正确行脚本内容待补录（从群历史可找回，未自动填入）；source_chat 已补，8/8 18:15 前会触发发布提醒。

---

## 18:21 · controller.py · 标已发容忍日期笔误（`_resolve_publish_row` 容错）

**老板反馈**（生产群 oc_03cf…，17:58）：发「8/3 19岁本科毕业，在新加坡能找到什么工作？真实工资单给你看 已发布@小席」→ 系统回「已发状态：未找到对应待发条目」。

**根因**：排期表该选题行是 8/8（当初按「排8/8 18:15 负责人张艺宝」补的正确行），用户确认时写 8/3 → `_resolve_publish_row` 里 `if date and r["date"] != date: continue` 日期不符直接跳过，不进 topic 模糊匹配 → 无匹配 → 明说未找到（符合「不误标」初衷，但对日期笔误过于严格）。

**改动**（`controller.py`）：
- `_resolve_publish_row` 返回 `(row, date_mismatch)`：日期对得上优先；日期对不上但标题在待产/待发行里**唯一命中**时容忍笔误（标记第二元素 True）；撞题歧义（多条同标题候选）仍返回 None 不猜。
- 新增 `_publish_reply`：日期不符时回执注明「你报的日期 8/3 表里是 8/8，已按表里 8/8 确认」，正常仍回「确认」。
- `_try_mark_published` 两处调用点改用新签名。

**测试**：test_controller 新增 2 条（唯一命中容忍笔误按 8/8 标已发+回执注明；撞题歧义仍回「未找到」不猜），174→176 全绿；scheduler 17 / intent 45 / session 6 回归全绿。

**运维**：小席整树重启（57890 → 65180，18:21 起）。生产验证：老板重发「8/8 19岁本科毕业… 已发布@小席」可正常标已发；发「8/3 …」也会标上并回执注明。

---

## 08-09 09:22 · controller.py + planner.py · 意图识别防误判（看表格硬锚 + 确认已发布防呆 + prompt 加固）

**老板反馈**（生产群 oc_03cf…，08-08 18:28）：发「看一下表格@小席」→ 小席回「已发状态：未找到对应待发条目」。

**根因**：意图识别 100% 押在 LLM 单点判断（planner.plan 一次 DeepSeek 调用 → 白名单 action → 角色门控 → 执行器），无关键词硬锚、无「动作与原文一致」校验、无后果分级。18:28:32 前 23 秒老板刚确认完一条「已发布」（历史全是发布确认语境），LLM 把「表格」顺着上下文读成「刚标那条的状态」→ 误判 action=确认已发布 → 执行标已发 → 无日期标题 → 回「未找到对应待发条目」。（日志 18:28:36「动作命中 action=确认已发布」）

**改动**：
- `controller.py` Fix A（看排期表硬锚）：新增 `_VIEW_SCHEDULE_RE`/`_view_schedule_requested`（含排除词：改/结构/加一列/删/标已发/已发布/标成），`handle_message` 在 planner 前拦截——明确含「看/查/打开+表格词」或「表格词+链接」→ 不依赖 LLM 直接走 `_exec_view_schedule`（复用 `_action_allowed` 权限 + `_schedule_reply` 链接回执）。
- `controller.py` Fix B（确认已发布防呆）：新增 `_PUBLISH_CONFIRM_WORDS`（已发布/已发/已确认发布/发布了一条/发布了/刚发布/刚发了/发了一条/发过了/发好了/发完了/发布完成/发出去了/这条发了/发了）+ `_has_publish_confirm` + `_publish_guard_reply`；`handle_message` 动作命中处：action=确认已发布 但原文无确认词 → 不执行，回「你是要标哪条已发布？说日期+标题」+ 排期表链接。
- `planner.py` Fix C（prompt 加固）：`_PLANNER_SYSTEM` 加「看表格/看排期 → 看排期表」「确认已发布必须原话含确认信号」两条。

**测试**：test_controller 新增 3 条（硬锚击败误判planner / 防呆拦截无确认词 / 确认词放行），174→177 全绿；scheduler 17 / intent 45 / session 6 回归全绿。

**运维**：小席整树重启（65180 → 77895，08-09 09:22 起）。
