# 标已发无日期无标题，不回最近定稿选题而是「没找到匹配的待发条目」

- **日期/群**：2026-08-10 16:40 · 测试群 oc_eed170…
- **状态**：已修复
- **触发输入**：已核对好，可以确定是已发了
- **实际行为**：小席回「已发状态：没找到匹配的待发条目，说清日期+标题」。老板刚定稿一个选题、刚上传多维表格，明明有上下文，小席却说不认识。
- **期望行为**：回落「最近定稿选题」（刚讨论的那条）定位待产/待发行并标已发，唯一命中即确认；撞题歧义才让老板补日期/标题。
- **根因**：`_try_mark_published` 只在消息里显式带「日期+标题」或标题能模糊命中时才定位行；消息无日期无标题 → 纯标题模糊匹配对不上 → 落「没找到」。执行器**没有回落 `_last_passed[chat_id]`**——这正是 8/8 排期意图兜底用的锚点（「可以了」定稿时 `_record_passed` 已写入 `_last_passed`），标已发这侧漏了。LLM 其实判对了 action=确认已发布（防呆「已发」也过了），缺的是执行器用上下文定位行。
- **修复**：`controller.py` `_try_mark_published(content, params, chat_id)` 新增 `_last_passed[chat_id]` 回落——无日期+标题也模糊不到时，用最近定稿选题归一化后对 待产/待发 行模糊匹配；**唯一命中才标已发**，撞题歧义（多条可对上）不猜、明说让老板补日期/标题。`_exec_mark_published` 传入 `msg.chat_id`。
- **回归测试**：test_controller 新增 2 条（`test_mark_published_uses_last_passed_context_no_date` 唯一命中→标已发回「确认」；`test_mark_published_last_passed_ambiguous_asks_not_guess` 撞题歧义→不标、让补日期标题），177→179 全绿。
- **关联**：变更记录 2026-08-10 16:50；架构书 §7.1「确认已发布」路由表 v1.9 + §9 排期表规则 4 加上下文回落；badcase 2026-08-08-看表格被误判确认已发布.md
