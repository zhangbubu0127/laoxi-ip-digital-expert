# 排期生成 3 条垃圾占位行（曝光选题1/2/3，负责人写死席小文）

- **日期/群**：2026-08-08 17:10 · 生产群 oc_03cf…
- **状态**：已修复
- **触发输入**：排8/8 18:15 负责人张艺宝
- **实际行为**：系统生成 3 条垃圾占位行（8/8 曝光选题1/2/3，负责人=席小文，类型=曝光），应为 1 条正确行（选题3=「19岁本科毕业，在新加坡能找到什么工作？真实工资单给你看」，类型=留资，负责人=张艺宝）。
- **期望行为**：落到刚定稿的那条选题，生成 1 条正确排期行（负责人按「负责人张艺宝」解析）。
- **根因**：17:07:04 脚本定稿 → 流水线进 `wait_schedule`，但 17:10:11 用户来排期已超 `_PIPE_TIMEOUT=180s`，`sweep_pipelines` 先清掉 wait_schedule → 消息落到 planner→改排期→`_exec_change_schedule`→`_try_pick_topic` 返回 None（「排8/8…」既不中 `_PASSED_HINTS` 也不中 `_PICK_HINTS`）→ `_schedule_entries` **count 默认 3、owner 写死席小文、type 默认曝光、topic 编造「曝光选题N」** 生成 3 条垃圾行。`_last_passed`（刚定稿选题）是模块态、流水线被 sweep 后仍在——修复落点。
- **修复**：`controller.py` 新增 `_parse_owner`/`_list_reference`/`_explicit_count`/`_has_schedule_intent`/`_parse_count`；`_try_pick_topic` ① 条件放宽（有 `_PASSED_HINTS` **或** 明确排期意图 → 用刚定稿选题，owner 走 `_parse_owner`）；`_schedule_entries` 只服务批量排期，无明确数量返回 `[]`，owner 不再写死席小文；`_exec_change_schedule` 无条目时明说「没指定排哪条选题或排几条」绝不编占位。
- **回归测试**：test_controller 新增 7 条（排期用 last_passed+date+owner / 批量 count 不误用 / 列表序号不误用 / entries 无数量空 / 内容带数量 3 条 / 无数量问细节不编占位 / lookup 优先 last_passed），165→172 全绿。
- **关联**：变更记录 2026-08-08 17:30；迭代记录 2026-08-09-排期意图修复与意图识别防误判.md 阶段 1；架构书 v1.8 排期规则
