# 看表格被误判成确认已发布，回「已发状态：未找到对应待发条目」

- **日期/群**：2026-08-08 18:28 · 生产群 oc_03cf…
- **状态**：已修复
- **触发输入**：看一下表格
- **实际行为**：18:28:32 老板发「看一下表格」→ 18:28:36 LLM 规划器判 `action=确认已发布` → 18:28:37 小席回「已发状态：未找到对应待发条目」（执行标已发逻辑，无日期标题 → 未找到）。
- **期望行为**：直接回多维表格链接。
- **根因**：意图识别 100% 押在 LLM 单点判断上（`planner.plan()` 一次 DeepSeek 调用 → `parse_plan` 白名单校验 → `_action_allowed` 角色门控 → `_ACTION_EXECUTORS` 执行），无关键词硬锚、无「动作与原文一致」校验、无后果分级。18:28:32 前 23 秒老板刚确认完一条「已发布」，历史全是发布确认语境，LLM 把「表格」顺着上下文读成「刚标那条的状态」→ 误判为确认已发布。确认已发布是高后果动作（排期表标已发），但内容无确认词时照样执行。
- **修复**：`controller.py` Fix A 看排期表硬锚（`_VIEW_SCHEDULE_RE`/`_view_schedule_requested` 含排除词 改/结构/加一列/删/标已发/已发布/标成，`handle_message` 在 planner 前拦截直接走 `_exec_view_schedule`）；Fix B 确认已发布防呆（`_has_publish_confirm` 校验原文含「已发布/发了」等确认词，无则不执行回「标哪条？说日期+标题」+ 链接）；`planner.py` Fix C prompt 加固两条。
- **回归测试**：test_controller 新增 3 条（硬锚击败误判 planner / 防呆拦截无确认词 / 确认词放行），174→177 全绿。
- **关联**：变更记录 2026-08-08 08-09 09:22；迭代记录 2026-08-09-排期意图修复与意图识别防误判.md 阶段 4；架构书 v1.8；badcase 2026-08-10-标已发无日期标题找不到.md
