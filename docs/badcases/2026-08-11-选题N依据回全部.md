# 「选题四的依据」回全部依据，开头还写「第 N 条对应第 N 个选题」

- **日期/群**：2026-08-11 14:26 · 测试群
- **状态**：已修复
- **触发输入**：给我选题四的依据
- **实际行为**：14:25 席小题刚出 5 条选题 + 存档依据，老板只问第 4 条 → 小席回**全部**依据，开头还带「第 N 条对应第 N 个选题」。
- **期望行为**：老板指了具体序号 → 只回那一条「选题N + 依据」，不给整版。
- **根因**：`_ask`（controller.py 1128 起）speaker==席小题 且有 basis 时直接 `load_basis` 全部 dump，不解析「选题四」序数——序号解析逻辑只在 skin 层 multi_main 有，controller 没复用。
- **修复**：新建 `src/brain/basis.py` 公共序号解析（`match_index`/`numbered_topics`/`basis_map`/`paired_topics`/`item_reply`，从 multi_main 迁出，多 bot 暂不动皮肤层）；`_ask` 改为 `match_index` 命中 → 只回那一条（`选题{n}「{题目}」\n依据：{reason}`）；序号解析不到 → 仍回全部依据兜底。
- **回归测试**：test_controller 新增 2 条（`test_ask_basis_picks_nth` 只回第 N 条、`test_ask_basis_fallback_all_when_no_index` 无序号仍回全部），181→184 全绿；scheduler 17 / intent 45 / session 6 / experts 34 回归全绿。
- **关联**：变更记录 2026-08-11；架构书 §7.2 追问路由。
