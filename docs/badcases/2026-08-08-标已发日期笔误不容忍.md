# 标已发日期笔误不容忍（8/3 实为 8/8），回「未找到对应待发条目」

- **日期/群**：2026-08-08 17:58 · 生产群 oc_03cf…
- **状态**：已修复
- **触发输入**：8/3 19岁本科毕业，在新加坡能找到什么工作？真实工资单给你看 已发布
- **实际行为**：小席回「已发状态：未找到对应待发条目」。排期表里该选题行是 8/8（当初按「排8/8 18:15 负责人张艺宝」补的正确行），老板确认时写 8/3 → 日期不符直接跳过，不进标题模糊匹配。
- **期望行为**：标题在待产/待发行唯一命中时容忍日期笔误，按表里 8/8 标已发并回执注明「你报的 8/3 表里是 8/8，已按表里确认」；撞题歧义才回未找到。
- **根因**：`_resolve_publish_row` 里 `if date and r["date"] != date: continue` 日期不符直接跳过，不进 topic 模糊匹配 → 无匹配 → 明说未找到。符合「不误标」初衷，但对日期笔误过于严格（唯一命中时本可安全放行）。
- **修复**：`controller.py` `_resolve_publish_row` 返回 `(row, date_mismatch)`——日期对得上优先；日期对不上但标题在待产/待发行**唯一命中**时容忍笔误（标记第二元素）；撞题歧义仍返回 None 不猜。新增 `_publish_reply`：日期不符时回执注明按表里日期确认。
- **回归测试**：test_controller 新增 2 条（唯一命中容忍笔误按 8/8 标已发+回执注明；撞题歧义仍回「未找到」不猜），174→176 全绿。
- **关联**：变更记录 2026-08-08 18:21；迭代记录 2026-08-09-排期意图修复与意图识别防误判.md 阶段 3；架构书 v1.8 规则 4
