# 查选题答非所问：问「我选择出脚本的选题」却 dump 整版 5 个选题

- **日期/群**：2026-08-08 17:12 · 生产群 oc_03cf…
- **状态**：已修复
- **触发输入**：我选择出脚本的选题你还能看到么
- **实际行为**：老板问「我选择（刚选中）出脚本的选题你还能看到么」→ 小席 dump 整版 5 个选题，不回刚定稿/选中的那一条。
- **期望行为**：回刚选中/刚定稿那条选题（`_last_passed`/`_draft_ctx`）。
- **根因**：planner 判「…还能看到么」为查记录 → `_lookup_topic_record` `force=True` 直接读 `load_basis` 整版 dump，不看 `_last_passed`。查记录不认「刚定稿」上下文。
- **修复**：`controller.py` `_lookup_topic_record` 新增 `_asks_chosen` 判定（选+脚本/选择/出脚本/定稿/刚通过 命中）→ 问的是刚选中/刚定稿那条时优先回 `_last_passed`/`_draft_ctx`，不回整版 dump。
- **回归测试**：test_controller 新增（lookup 优先 last_passed，随排期修复一批共 7 条），165→172 全绿。
- **关联**：变更记录 2026-08-08 17:30（Bug 1）；迭代记录 2026-08-09-排期意图修复与意图识别防误判.md 阶段 2；架构书 v1.8
